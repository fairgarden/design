import { createDemo } from '@/functions/createDemo';
import { TooltipGrounds } from './TooltipGrounds';

export const DemoTooltipGrounds = createDemo(import.meta.url, TooltipGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
