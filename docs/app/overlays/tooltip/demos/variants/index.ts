import { createDemo } from '@/functions/createDemo';
import { TooltipVariants } from './TooltipVariants';

export const DemoTooltipVariants = createDemo(import.meta.url, TooltipVariants, {
  name: 'Label, hint and term',
  slug: 'variants',
});
