import { createDemo } from '@/functions/createDemo';
import { CheckboxColor } from './CheckboxColor';

export const DemoCheckboxColor = createDemo(import.meta.url, CheckboxColor, {
  name: 'Primary and secondary',
  slug: 'color',
});
