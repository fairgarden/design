import { createDemo } from '@/functions/createDemo';
import { CheckboxKinds } from './CheckboxKinds';

export const DemoCheckboxKinds = createDemo(import.meta.url, CheckboxKinds, {
  name: 'Option cards and ledger rows',
  slug: 'kinds',
});
