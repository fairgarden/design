import { createDemo } from '@/functions/createDemo';
import { CheckboxStates } from './CheckboxStates';

export const DemoCheckboxStates = createDemo(import.meta.url, CheckboxStates, {
  name: 'States',
  slug: 'states',
});
