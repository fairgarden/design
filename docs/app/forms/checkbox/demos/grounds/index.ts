import { createDemo } from '@/functions/createDemo';
import { CheckboxGrounds } from './CheckboxGrounds';

export const DemoCheckboxGrounds = createDemo(import.meta.url, CheckboxGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
