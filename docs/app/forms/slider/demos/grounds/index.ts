import { createDemo } from '@/functions/createDemo';
import { SliderGrounds } from './SliderGrounds';

export const DemoSliderGrounds = createDemo(import.meta.url, SliderGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
