import { createDemo } from '@/functions/createDemo';
import { ComboboxGrounds } from './ComboboxGrounds';

export const DemoComboboxGrounds = createDemo(import.meta.url, ComboboxGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
