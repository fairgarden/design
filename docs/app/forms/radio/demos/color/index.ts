import { createDemo } from '@/functions/createDemo';
import { RadioColor } from './RadioColor';

export const DemoRadioColor = createDemo(import.meta.url, RadioColor, {
  name: 'Primary and secondary',
  slug: 'color',
});
