# Radio

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate forms/radio'

## API Reference

### Radio

A Base UI Radio inside its own label row, so the whole row is the hit
target. Always inside a RadioGroup. Checked is a --role-select fill, an
8 px ● in --role-select-mark and a 3 px edge, never fill alone \[D15].

**Radio Props:**

| Prop        | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                                                                                                                                        |
| :---------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| description | `React.ReactNode`                                                                                                                                                                                                                                                                                                                                  | -       | Optional helper text under the label, in sentence case.                                                                                                                                                                            |
| feedback    | `React.ReactNode`                                                                                                                                                                                                                                                                                                                                  | -       | Quiz feedback shown after the label: a RadioFeedback.                                                                                                                                                                              |
| kind        | `'standard' \| 'pill' \| 'swatch' \| null`                                                                                                                                                                                                                                                                                                         | -       | `standard` (default): circle plus label. `pill`: a stacked&#xA;`--size-px-8` option pill. `swatch`: a color disc whose name is the&#xA;accessible label (shown in print). Inside a RadioGroup the group's kind&#xA;is the default. |
| primary     | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale: circle edge, labels, swatch rings and focus ring.&#xA;Never defaulted; omitted, it inherits the scope \[D133].                                                                                                |
| secondary   | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale: the checked fill, dot and edge through&#xA;--role-select. Omitted, it inherits the scope. Never the danger scale.                                                                                           |
| swatch      | `string`                                                                                                                                                                                                                                                                                                                                           | -       | `swatch` only: the disc's color or image, as a CSS `background` value.&#xA;The color is content, not a role; its name is `children`.                                                                                               |
| children    | `React.ReactNode`                                                                                                                                                                                                                                                                                                                                  | -       | The label, authored in title case up to about four words \[D160]; a swatch's color name.                                                                                                                                           |
| className   | `string`                                                                                                                                                                                                                                                                                                                                           | -       | Class for the row (the `base` part).                                                                                                                                                                                               |

### RadioFeedback

Quiz feedback for a Radio's `feedback` slot: the §1.5.4 success glyph
(● with its check) or danger glyph (◆ with its ×) in --role-status,
always paired with a word, so fill is never the only signal \[D58].

**RadioFeedback Props:**

| Prop      | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                 |
| :-------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------- |
| primary   | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale: the word. Never defaulted \[D133].                     |
| secondary | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale: the glyph. Omitted, the status scale (green or red). |
| status    | `RadioFeedbackStatus`                                                                                                                                                                                                                                                                                                                              | -       | `success` for the correct answer, `danger` for a wrong pick.                |
| children  | `React.ReactNode`                                                                                                                                                                                                                                                                                                                                  | -       | The word; defaults to "Correct" or "Not quite".                             |

### RadioGroup

One choice from 2–7 visible options (Base UI Radio Group). Arrow keys
move the selection; Tab leaves the group. Preselect a sensible default
unless the choice must be deliberate. Eight or more options are a Select.
For swatches, let the legend name the choice ("Color: Forest"). The root
is an inline-size container, so give it a width in shrink-to-fit layouts.

**RadioGroup Props:**

| Prop      | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                                                                                                          |
| :-------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| kind      | `'standard' \| 'pill' \| 'swatch' \| null`                                                                                                                                                                                                                                                                                                         | -       | The build of the radios inside, and so the layout: `standard`&#xA;(default), `pill` (option pills, always stacked) or `swatch` (color&#xA;discs that wrap). Each Radio takes it as its default kind. |
| primary   | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale for every radio inside. Never defaulted \[D133].                                                                                                                                 |
| secondary | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale for every radio inside. Never defaulted, never the danger scale.                                                                                                               |

## Additional Types

### radio

```typescript
type radio = radio;
```

### radioFeedback

```typescript
type radioFeedback = radioFeedback;
```

### RadioFeedbackProps

Props for RadioFeedback: `span` props, the status, the word and the color axes.

```typescript
type RadioFeedbackProps = {
  /** `success` for the correct answer, `danger` for a wrong pick. */
  status: RadioFeedbackStatus;
  /** Primary Radix scale: the word. Never defaulted [D133]. */
  primary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | null;
  /** Secondary Radix scale: the glyph. Omitted, the status scale (green or red). */
  secondary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | 'amber'
    | 'blue'
    | 'cyan'
    | 'grass'
    | 'green'
    | 'jade'
    | 'lime'
    | 'mint'
    | 'orange'
    | 'sky'
    | 'teal'
    | 'yellow'
    | null;
  /** The word; defaults to "Correct" or "Not quite". */
  children?: React.ReactNode;
};
```

### RadioFeedbackStatus

Quiz feedback status (§10.8).

```typescript
type RadioFeedbackStatus = 'success' | 'danger';
```

### radioGroup

```typescript
type radioGroup = radioGroup;
```

### RadioGroupProps

Props for RadioGroup: Base UI RadioGroup props plus the kind and color
axes. Label the group with a Fieldset legend or `aria-labelledby`.

```typescript
type RadioGroupProps<Value = unknown> = RadioGroup.Props<Value> & {
  kind?: 'standard' | 'pill' | 'swatch' | null;
  primary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | null;
  secondary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | 'amber'
    | 'blue'
    | 'cyan'
    | 'grass'
    | 'green'
    | 'jade'
    | 'lime'
    | 'mint'
    | 'orange'
    | 'sky'
    | 'teal'
    | 'yellow'
    | null;
};
```

### RadioKind

The structural build of a Radio (§10.8).

```typescript
type RadioKind = NonNullable<'standard' | 'pill' | 'swatch' | null | undefined>;
```

### RadioKindContext

The kind a RadioGroup passes to the radios inside it. A radio's own `kind` wins.

```typescript
type RadioKindContext = React.Context<RadioKind | undefined>;
```

### RadioProps

Props for Radio: Base UI Radio.Root props (on the circle) plus the kind,
color axes and the row's content. `className` goes on the row.

```typescript
type RadioProps<Value = unknown> = {
  /**
   * `standard` (default): circle plus label. `pill`: a stacked
   * `--size-px-8` option pill. `swatch`: a color disc whose name is the
   * accessible label (shown in print). Inside a RadioGroup the group's kind
   * is the default.
   */
  kind?: 'standard' | 'pill' | 'swatch' | null;
  /**
   * Primary Radix scale: circle edge, labels, swatch rings and focus ring.
   * Never defaulted; omitted, it inherits the scope [D133].
   */
  primary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | null;
  /**
   * Secondary Radix scale: the checked fill, dot and edge through
   * --role-select. Omitted, it inherits the scope. Never the danger scale.
   */
  secondary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | 'amber'
    | 'blue'
    | 'cyan'
    | 'grass'
    | 'green'
    | 'jade'
    | 'lime'
    | 'mint'
    | 'orange'
    | 'sky'
    | 'teal'
    | 'yellow'
    | null;
  /** Class for the row (the `base` part). */
  className?: string;
  /** The label, authored in title case up to about four words [D160]; a swatch's color name. */
  children?: React.ReactNode;
  /** Optional helper text under the label, in sentence case. */
  description?: React.ReactNode;
  /**
   * `swatch` only: the disc's color or image, as a CSS `background` value.
   * The color is content, not a role; its name is `children`.
   */
  swatch?: string;
  /** Quiz feedback shown after the label: a RadioFeedback. */
  feedback?: React.ReactNode;
};
```

## External Types

### NonNullable

```typescript
type NonNullable = 'standard' | 'pill' | 'swatch';
```

## Export Groups

- `Radio`: `Radio`, `RadioFeedback`, `radio`, `RadioKind`, `RadioKindContext`, `RadioProps`, `RadioFeedbackStatus`, `radioFeedback`, `RadioFeedbackProps`
- `RadioGroup`: `RadioGroup`, `radioGroup`, `RadioGroupProps`
- `RadioFeedback`: `Radio`, `RadioFeedback`, `radio`, `RadioKind`, `RadioKindContext`, `RadioProps`, `RadioFeedbackStatus`, `radioFeedback`, `RadioFeedbackProps`
