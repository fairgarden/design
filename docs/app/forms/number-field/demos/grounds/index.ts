import { createDemo } from '@/functions/createDemo';
import { NumberFieldGrounds } from './NumberFieldGrounds';

export const DemoNumberFieldGrounds = createDemo(import.meta.url, NumberFieldGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
