import { createDemo } from '@/functions/createDemo';
import { FieldsetColor } from './FieldsetColor';

export const DemoFieldsetColor = createDemo(import.meta.url, FieldsetColor, {
  name: 'Primary scale',
  slug: 'color',
});
