import { createDemo } from '@/functions/createDemo';
import { FieldsetGrounds } from './FieldsetGrounds';

export const DemoFieldsetGrounds = createDemo(import.meta.url, FieldsetGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
