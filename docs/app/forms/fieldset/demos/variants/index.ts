import { createDemo } from '@/functions/createDemo';
import { FieldsetVariants } from './FieldsetVariants';

export const DemoFieldsetVariants = createDemo(import.meta.url, FieldsetVariants, {
  name: 'Variants and states',
  slug: 'variants',
});
