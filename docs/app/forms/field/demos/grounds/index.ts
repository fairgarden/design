import { createDemo } from '@/functions/createDemo';
import { FieldGrounds } from './FieldGrounds';

export const DemoFieldGrounds = createDemo(import.meta.url, FieldGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
