import { createDemo } from '@/functions/createDemo';
import { InputGrounds } from './InputGrounds';

export const DemoInputGrounds = createDemo(import.meta.url, InputGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
