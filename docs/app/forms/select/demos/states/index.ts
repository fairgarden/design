import { createDemo } from '@/functions/createDemo';
import { SelectStates } from './SelectStates';

export const DemoSelectStates = createDemo(import.meta.url, SelectStates, {
  name: 'Variants and states',
  slug: 'states',
});
