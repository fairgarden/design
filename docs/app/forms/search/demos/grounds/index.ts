import { createDemo } from '@/functions/createDemo';
import { SearchGrounds } from './SearchGrounds';

export const DemoSearchGrounds = createDemo(import.meta.url, SearchGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
