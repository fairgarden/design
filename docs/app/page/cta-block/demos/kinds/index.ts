import { createDemo } from '@/functions/createDemo';
import { CTABlockKinds } from './CTABlockKinds';

export const DemoCTABlockKinds = createDemo(import.meta.url, CTABlockKinds, {
  name: 'Kinds',
  slug: 'kinds',
});
