import { createTypes } from '@/functions/createTypes';
import { SectionHeader } from '@fairgarden/design/page/section-header';

export const TypesSectionHeader = createTypes(import.meta.url, SectionHeader);
