# Announcement Bar

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate page/announcement-bar'

## API Reference

### AnnouncementBar

The announcement bar. Never sticky, never rotating, never truncated with
an ellipsis; it scrolls away with the page. Hidden in print unless it is
a `record`, whose countdown prints its end date.

**AnnouncementBar Props:**

| Prop         | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                                                                                                                        |
| :----------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| countdown    | `AnnouncementCountdown`                                                                                                                                                                                                                                                                                                                            | -       | Boxed countdown digits; a polite live region announces at most once a minute.                                                                                                                                      |
| dismissLabel | `string`                                                                                                                                                                                                                                                                                                                                           | -       | The dismiss button's accessible name. Default "Dismiss announcement".                                                                                                                                              |
| dismissible  | `boolean`                                                                                                                                                                                                                                                                                                                                          | -       | Adds the dismiss button (`close`, in a 44 px target). Default `false`.                                                                                                                                             |
| kind         | `'bar' \| 'field'`                                                                                                                                                                                                                                                                                                                                 | -       | `bar` (default): a full-bleed page-ground bar with its bottom rule.&#xA;`field`: a full-container field with its `--primary12` edge \[D180].                                                                       |
| link         | `{ href: string; label: React.ReactNode }`                                                                                                                                                                                                                                                                                                         | -       | The one link, after the message: underlined at rest in `--role-accent`&#xA;(§9.3 body link); it prints its short URL.                                                                                              |
| onDismiss    | `(() => void)`                                                                                                                                                                                                                                                                                                                                     | -       | Called after the bar is dismissed.                                                                                                                                                                                 |
| preset       | `'paper' \| 'white' \| 'tide' \| 'meadow' \| 'pollen' \| 'apricot' \| 'rose' \| 'heather' \| AnnouncementField`                                                                                                                                                                                                                                    | -       | The bar's page ground: `white`, `paper` or the page's pastel. Omitted,&#xA;the bar sits on the surrounding page ground.                                                                                            |
| primary      | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale: message, digits, cells, glyph and focus ring. Never&#xA;defaulted; omitted, it inherits the bar's preset \[D133].                                                                             |
| record       | `boolean`                                                                                                                                                                                                                                                                                                                                          | -       | A notice of record (a legal change, a recall): it prints once, between&#xA;two 0.75 pt rules. Default `false`: the bar is hidden in print.                                                                         |
| ruled        | `boolean`                                                                                                                                                                                                                                                                                                                                          | -       | The bottom rule is required on every page-ground bar.                                                                                                                                                              |
| secondary    | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale: the link underline and its hover ink. Never defaulted.                                                                                                                                      |
| storageKey   | `string`                                                                                                                                                                                                                                                                                                                                           | -       | Remembers the dismissal per viewer under this key (browser storage), so&#xA;the bar stays gone on later visits. Change the key for a new notice.                                                                   |
| voice        | `'standard' \| 'mono' \| 'dual' \| null`                                                                                                                                                                                                                                                                                                           | -       | `standard` (default): caps `type-label`, centered. `mono`: `type-data`&#xA;caps for dated or technical notices ("CLOSED 24–26 DEC"). `dual`: the&#xA;caps run plus one serif caps phrase, `AnnouncementBarAccent`. |
| children\*   | `React.ReactNode`                                                                                                                                                                                                                                                                                                                                  | -       | The message: one short notice, authored; ≤ 2 lines at base, 1 from `--md-n-above`.                                                                                                                                 |

### AnnouncementBarAccent

The dual voice's one serif caps phrase, set inside the message at the
run's cap height (Rocco's two-voice bar). Use it once, with `voice="dual"`.

## Additional Types

### announcementBar

```typescript
type announcementBar = announcementBar;
```

### AnnouncementBarAccentProps

Props for AnnouncementBarAccent: `span` props.

```typescript
type AnnouncementBarAccentProps = React.ComponentPropsWithRef<'span'>;
```

### AnnouncementBarProps

Props for AnnouncementBar: `aside` props, the message, link, voice,
record, countdown and dismissal, the kind with its preset, and the color
axes. `ruled` is required with `kind: bar` and excluded with `kind: field`.

```typescript
type AnnouncementBarProps = AnnouncementBarBarProps | AnnouncementBarFieldProps;
```

### AnnouncementCountdown

The countdown: boxed digits to an end time, which print as the end date.

```typescript
type AnnouncementCountdown = {
  /** The end, a Date or an ISO 8601 string. */
  end: Date | string;
  /** The text that replaces the digits at zero, e.g. "Sale ended". */
  expired: React.ReactNode;
  /**
   * The printed end date line. Default "Ends 30 Sept 2026", formatted from
   * `end` in `locale` and the reader's time zone once the page has loaded.
   */
  endLabel?: string;
  /** The locale for the default end label. Default `en-GB`. */
  locale?: string;
  /** Unit labels after the day, hour and minute cells. Default `d`, `h`, `min`. */
  units?: [string, string, string];
};
```

### AnnouncementField

The fields a full-container bar may take \[D180]: never `pink`, never `royal`.

```typescript
type AnnouncementField = 'forest' | 'leaf' | 'amber' | 'brick';
```

## Export Groups

- `AnnouncementBar`: `AnnouncementBar`, `AnnouncementBarAccent`, `announcementBar`, `AnnouncementField`, `AnnouncementCountdown`, `AnnouncementBarProps`, `AnnouncementBarAccentProps`
- `AnnouncementBarAccent`: `AnnouncementBar`, `AnnouncementBarAccent`, `announcementBar`, `AnnouncementField`, `AnnouncementCountdown`, `AnnouncementBarProps`, `AnnouncementBarAccentProps`
