import { createDemo } from '@/functions/createDemo';
import { AnnouncementBarBars } from './AnnouncementBarBars';

export const DemoAnnouncementBarBars = createDemo(import.meta.url, AnnouncementBarBars, {
  name: 'Bars and fields',
  slug: 'bars',
});
