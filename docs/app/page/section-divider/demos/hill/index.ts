import { createDemo } from '@/functions/createDemo';
import { SectionDividerHill } from './SectionDividerHill';

export const DemoSectionDividerHill = createDemo(import.meta.url, SectionDividerHill, {
  name: 'The hill',
  slug: 'hill',
});
