import { createDemo } from '@/functions/createDemo';
import { SectionDividerSeams } from './SectionDividerSeams';

export const DemoSectionDividerSeams = createDemo(import.meta.url, SectionDividerSeams, {
  name: 'Seams and pauses',
  slug: 'seams',
});
