# Profile

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate content/profile'

## API Reference

### Profile

The profile module. A byline holds `ProfilePortrait`, `ProfileName` and
`ProfileDate`; an author block `ProfilePortrait`, `ProfileName`,
`ProfileBio`; a bio row `ProfilePortrait` and `ProfileDetails` (name,
role, bio, links); a team `ProfileTeam` of `ProfileMember`s, each with a
`ProfileTeamPhoto`, `ProfileName` and `ProfileRole`. Use real portraits
or initials; never overlay names on photos, never dim former members.

**Profile Props:**

| Prop      | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                                                                                                                                                                                                                                                           |
| :-------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| kind\*    | `ProfileKind`                                                                                                                                                                                                                                                                                                                                      | -       | Required. `byline`: inline, a 32 px portrait, "By Name" and the date&#xA;joined by "·". `author`: the end-of-article block, portrait → serif&#xA;name → description. `bio`: portrait at the start (64 px, 128 px from&#xA;768 px of container) beside the heading and body, even on phones.&#xA;`team`: a grid of square photos with names and roles. |
| primary   | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale: rings, frames, initials and text. Never&#xA;defaulted; omitted, it inherits the scope \[D133].                                                                                                                                                                                                                                   |
| secondary | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale: only the name-link and social-link underlines&#xA;(`--role-accent`). Never defaulted.                                                                                                                                                                                                                                          |

### ProfileBio

A short bio or description, 3–5 lines of `type-body-ui` in `--primary12`; prints in full.

### ProfileDate

The byline date, after a "·" separator: `type-caption` in `--primary12`. Pass `dateTime`.

### ProfileDetails

The bio row's text column beside the portrait: name, role, bio and links.

### ProfileLinks

Social and contact links, a `ul` of `li > Link`: text links with the
`--role-accent` underline, or inline-tier icons. They sit above a
stretched name link as their own tab stops. In print, write handles in
text; icon-only links are omitted.

### ProfileMember

One team member: photo → name → role. A `ProfileNameLink` links the whole cell.

### ProfileName

The name in `--primary12`: `type-itemhead` for the author block, bio row
and team, `type-body-ui` at `--font-weight-6` in a byline ("By Name").
Renders `<h3>`, or `<span>` in a byline. Wrap a `ProfileNameLink` inside
to link the profile.

### ProfileNameLink

The profile's one link, inside `ProfileName`: a §9.3 title Link whose hit
area stretches over the member (or the whole profile). Hover, focus and
press underline the name; the item draws the focus ring.

**ProfileNameLink Props:**

| Prop      | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| :-------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| external  | `boolean \| null`                                                                                                                                                                                                                                                                                                                                  | -       | Adds the arrow-open mark and "(external site)" for assistive technology.&#xA;Default `false`.                                                                                                                                                                                                                                                                                                                                                 |
| index     | `boolean \| null`                                                                                                                                                                                                                                                                                                                                  | -       | Adds the screen-only visited ✓ used in long indexes, such as reference&#xA;and archive lists. Default `false` \[D174, D175].                                                                                                                                                                                                                                                                                                                  |
| list      | `boolean \| null`                                                                                                                                                                                                                                                                                                                                  | -       | With `kind="nav"`: a list link (nav-panel, footer, drawer and breadcrumb&#xA;lists), whose hover is `--role-link-hover` color only, plus the&#xA;`--ds-stroke-1-5` `--role-accent` underline where that ink is&#xA;`--primary12`. Without it, `nav` is bare navigation text (bar and&#xA;utility items, page numbers, toolbar links), whose hover is the&#xA;`--border-size-2` `--role-accent` underline (§9.3) \[D181]. Default&#xA;`false`. |
| muted     | `boolean \| null`                                                                                                                                                                                                                                                                                                                                  | -       | Rests in `--role-muted` instead of `--primary12`, as a breadcrumb's&#xA;ancestors. Hover takes `--role-link-hover` only (the underline is added&#xA;where that ink is `--primary12`); with `kind="nav"` it replaces the bar&#xA;item's bare-text underline (§9.3, §9.8) \[D181]. Default `false`.                                                                                                                                             |
| primary   | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale: the link text and focus ring. Never defaulted;&#xA;omitted, it inherits the scope \[D133].                                                                                                                                                                                                                                                                                                                               |
| secondary | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale: the accent underline. Never defaulted; omitted,&#xA;it inherits the scope.                                                                                                                                                                                                                                                                                                                                             |
| className | `string`                                                                                                                                                                                                                                                                                                                                           | -       | Extra class names, added after the module's own.                                                                                                                                                                                                                                                                                                                                                                                              |

### ProfilePortrait

The portrait: an Avatar sized by the kind (byline 32 px, author 40 px,
bio 64 px stepping to 128 px from 768 px of container), with its ring per
§10.12, taking the module's `primary` and `secondary` unless given its
own. Compose `AvatarImage` (empty alt where the name is adjacent) and
`AvatarFallback` (1–2 initials) inside; the fallback keeps the spacing.

**ProfilePortrait Props:**

| Prop      | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                                                                                                                   |
| :-------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| primary   | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale: the ring. Never defaulted; omitted, it inherits&#xA;the scope \[D133].                                                                                                                   |
| secondary | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale, accepted for the shared contract; no part uses it.                                                                                                                                     |
| size      | `'xs' \| 'sm' \| 'md' \| 'lg' \| 'xl' \| null`                                                                                                                                                                                                                                                                                                     | -       | `xs` 24, `sm` 32, `md` 40 (default), `lg` 64, `xl` 128 px. Up to 40 px&#xA;the ring is `--border-size-1` `--role-rule`; from 64 px it is&#xA;`--border-size-2` `--primary12`. Sizes are fixed at every width. |

### ProfileRole

Role or affiliation: `type-label` caps in `--primary12`. Former members
say so in words ("Former board chair"); never dim them.

### ProfileTeam

The team grid: 1-up below 360 px of container, 2-up from 360, 4-up from
768, up to 5-up from 1024 (auto-fit at 152 px without container-query
support). Prints 3-up.

### ProfileTeamPhoto

A team member's photo: a plain `img`, square at the cell's full width,
`--ds-radius-8`, framed in a `--border-size-1` `--role-hairline` line.
Give it empty alt when the name follows.

## Additional Types

### profile

```typescript
type profile = profile;
```

### ProfileBioProps

Props for ProfileBio: `div` props and `render`.

```typescript
type ProfileBioProps = useRender.ComponentProps<'div'>;
```

### ProfileDateProps

Props for ProfileDate: `time` props and `render`.

```typescript
type ProfileDateProps = useRender.ComponentProps<'time'>;
```

### ProfileDetailsProps

Props for ProfileDetails: `div` props and `render`.

```typescript
type ProfileDetailsProps = useRender.ComponentProps<'div'>;
```

### ProfileKind

The four profile builds (§12.15).

```typescript
type ProfileKind = 'byline' | 'author' | 'bio' | 'team';
```

### ProfileLinksProps

Props for ProfileLinks: list props and `render`.

```typescript
type ProfileLinksProps = useRender.ComponentProps<'ul'>;
```

### ProfileMemberProps

Props for ProfileMember: list-item props and `render`.

```typescript
type ProfileMemberProps = useRender.ComponentProps<'li'>;
```

### ProfileNameLinkProps

Props for ProfileNameLink: Link props except `kind`, which is always `title`.

```typescript
type ProfileNameLinkProps = {
  /** Extra class names, added after the module's own. */
  className?: string;
  /**
   * Primary Radix scale: the link text and focus ring. Never defaulted;
   * omitted, it inherits the scope [D133].
   */
  primary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | null;
  /**
   * Secondary Radix scale: the accent underline. Never defaulted; omitted,
   * it inherits the scope.
   */
  secondary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | 'amber'
    | 'blue'
    | 'cyan'
    | 'grass'
    | 'green'
    | 'jade'
    | 'lime'
    | 'mint'
    | 'orange'
    | 'sky'
    | 'teal'
    | 'yellow'
    | null;
  /**
   * With `kind="nav"`: a list link (nav-panel, footer, drawer and breadcrumb
   * lists), whose hover is `--role-link-hover` color only, plus the
   * `--ds-stroke-1-5` `--role-accent` underline where that ink is
   * `--primary12`. Without it, `nav` is bare navigation text (bar and
   * utility items, page numbers, toolbar links), whose hover is the
   * `--border-size-2` `--role-accent` underline (§9.3) [D181]. Default
   * `false`.
   */
  list?: boolean | null;
  /**
   * Adds the screen-only visited ✓ used in long indexes, such as reference
   * and archive lists. Default `false` [D174, D175].
   */
  index?: boolean | null;
  /**
   * Adds the arrow-open mark and "(external site)" for assistive technology.
   * Default `false`.
   */
  external?: boolean | null;
  /**
   * Rests in `--role-muted` instead of `--primary12`, as a breadcrumb's
   * ancestors. Hover takes `--role-link-hover` only (the underline is added
   * where that ink is `--primary12`); with `kind="nav"` it replaces the bar
   * item's bare-text underline (§9.3, §9.8) [D181]. Default `false`.
   */
  muted?: boolean | null;
};
```

### ProfileNameProps

Props for ProfileName: heading props and `render` (default `<h3>`; `<span>` in a byline).

```typescript
type ProfileNameProps = useRender.ComponentProps<'h3'>;
```

### ProfilePortraitProps

Props for ProfilePortrait: Avatar props; `size` follows the kind unless given.

```typescript
type ProfilePortraitProps = AvatarProps;
```

### ProfileProps

Props for Profile: `div` props, `render`, the kind and the color axes.

```typescript
type ProfileProps = React.ClassAttributes<HTMLDivElement> &
  React.HTMLAttributes<HTMLDivElement> &
  __type & {
    kind: ProfileKind;
    primary?:
      | 'ruby'
      | 'olive'
      | 'sage'
      | 'slate'
      | 'sand'
      | 'gray'
      | 'mauve'
      | 'brown'
      | 'bronze'
      | 'gold'
      | 'red'
      | 'crimson'
      | 'tomato'
      | 'pink'
      | 'plum'
      | 'indigo'
      | 'iris'
      | 'violet'
      | 'purple'
      | null;
    secondary?:
      | 'ruby'
      | 'olive'
      | 'sage'
      | 'slate'
      | 'sand'
      | 'gray'
      | 'mauve'
      | 'brown'
      | 'bronze'
      | 'gold'
      | 'red'
      | 'crimson'
      | 'tomato'
      | 'pink'
      | 'plum'
      | 'indigo'
      | 'iris'
      | 'violet'
      | 'purple'
      | 'amber'
      | 'blue'
      | 'cyan'
      | 'grass'
      | 'green'
      | 'jade'
      | 'lime'
      | 'mint'
      | 'orange'
      | 'sky'
      | 'teal'
      | 'yellow'
      | null;
  };
```

### ProfileRoleProps

Props for ProfileRole: paragraph props and `render`.

```typescript
type ProfileRoleProps = useRender.ComponentProps<'p'>;
```

### ProfileTeamPhotoProps

Props for ProfileTeamPhoto: `img` props and `render`.

```typescript
type ProfileTeamPhotoProps = useRender.ComponentProps<'img'>;
```

### ProfileTeamProps

Props for ProfileTeam: list props and `render`.

```typescript
type ProfileTeamProps = useRender.ComponentProps<'ul'>;
```

## Export Groups

- `Profile`: `Profile`, `ProfilePortrait`, `ProfileTeamPhoto`, `ProfileDetails`, `ProfileName`, `ProfileNameLink`, `ProfileRole`, `ProfileBio`, `ProfileLinks`, `ProfileDate`, `ProfileTeam`, `ProfileMember`, `profile`, `ProfileKind`, `ProfileProps`, `ProfilePortraitProps`, `ProfileTeamPhotoProps`, `ProfileDetailsProps`, `ProfileNameProps`, `ProfileNameLinkProps`, `ProfileRoleProps`, `ProfileBioProps`, `ProfileLinksProps`, `ProfileDateProps`, `ProfileTeamProps`, `ProfileMemberProps`
- `ProfilePortrait`: `Profile`, `ProfilePortrait`, `ProfileTeamPhoto`, `ProfileDetails`, `ProfileName`, `ProfileNameLink`, `ProfileRole`, `ProfileBio`, `ProfileLinks`, `ProfileDate`, `ProfileTeam`, `ProfileMember`, `profile`, `ProfileKind`, `ProfileProps`, `ProfilePortraitProps`, `ProfileTeamPhotoProps`, `ProfileDetailsProps`, `ProfileNameProps`, `ProfileNameLinkProps`, `ProfileRoleProps`, `ProfileBioProps`, `ProfileLinksProps`, `ProfileDateProps`, `ProfileTeamProps`, `ProfileMemberProps`
- `ProfileTeamPhoto`: `Profile`, `ProfilePortrait`, `ProfileTeamPhoto`, `ProfileDetails`, `ProfileName`, `ProfileNameLink`, `ProfileRole`, `ProfileBio`, `ProfileLinks`, `ProfileDate`, `ProfileTeam`, `ProfileMember`, `profile`, `ProfileKind`, `ProfileProps`, `ProfilePortraitProps`, `ProfileTeamPhotoProps`, `ProfileDetailsProps`, `ProfileNameProps`, `ProfileNameLinkProps`, `ProfileRoleProps`, `ProfileBioProps`, `ProfileLinksProps`, `ProfileDateProps`, `ProfileTeamProps`, `ProfileMemberProps`
- `ProfileDetails`: `Profile`, `ProfilePortrait`, `ProfileTeamPhoto`, `ProfileDetails`, `ProfileName`, `ProfileNameLink`, `ProfileRole`, `ProfileBio`, `ProfileLinks`, `ProfileDate`, `ProfileTeam`, `ProfileMember`, `profile`, `ProfileKind`, `ProfileProps`, `ProfilePortraitProps`, `ProfileTeamPhotoProps`, `ProfileDetailsProps`, `ProfileNameProps`, `ProfileNameLinkProps`, `ProfileRoleProps`, `ProfileBioProps`, `ProfileLinksProps`, `ProfileDateProps`, `ProfileTeamProps`, `ProfileMemberProps`
- `ProfileName`: `Profile`, `ProfilePortrait`, `ProfileTeamPhoto`, `ProfileDetails`, `ProfileName`, `ProfileNameLink`, `ProfileRole`, `ProfileBio`, `ProfileLinks`, `ProfileDate`, `ProfileTeam`, `ProfileMember`, `profile`, `ProfileKind`, `ProfileProps`, `ProfilePortraitProps`, `ProfileTeamPhotoProps`, `ProfileDetailsProps`, `ProfileNameProps`, `ProfileNameLinkProps`, `ProfileRoleProps`, `ProfileBioProps`, `ProfileLinksProps`, `ProfileDateProps`, `ProfileTeamProps`, `ProfileMemberProps`
- `ProfileNameLink`: `Profile`, `ProfilePortrait`, `ProfileTeamPhoto`, `ProfileDetails`, `ProfileName`, `ProfileNameLink`, `ProfileRole`, `ProfileBio`, `ProfileLinks`, `ProfileDate`, `ProfileTeam`, `ProfileMember`, `profile`, `ProfileKind`, `ProfileProps`, `ProfilePortraitProps`, `ProfileTeamPhotoProps`, `ProfileDetailsProps`, `ProfileNameProps`, `ProfileNameLinkProps`, `ProfileRoleProps`, `ProfileBioProps`, `ProfileLinksProps`, `ProfileDateProps`, `ProfileTeamProps`, `ProfileMemberProps`
- `ProfileRole`: `Profile`, `ProfilePortrait`, `ProfileTeamPhoto`, `ProfileDetails`, `ProfileName`, `ProfileNameLink`, `ProfileRole`, `ProfileBio`, `ProfileLinks`, `ProfileDate`, `ProfileTeam`, `ProfileMember`, `profile`, `ProfileKind`, `ProfileProps`, `ProfilePortraitProps`, `ProfileTeamPhotoProps`, `ProfileDetailsProps`, `ProfileNameProps`, `ProfileNameLinkProps`, `ProfileRoleProps`, `ProfileBioProps`, `ProfileLinksProps`, `ProfileDateProps`, `ProfileTeamProps`, `ProfileMemberProps`
- `ProfileBio`: `Profile`, `ProfilePortrait`, `ProfileTeamPhoto`, `ProfileDetails`, `ProfileName`, `ProfileNameLink`, `ProfileRole`, `ProfileBio`, `ProfileLinks`, `ProfileDate`, `ProfileTeam`, `ProfileMember`, `profile`, `ProfileKind`, `ProfileProps`, `ProfilePortraitProps`, `ProfileTeamPhotoProps`, `ProfileDetailsProps`, `ProfileNameProps`, `ProfileNameLinkProps`, `ProfileRoleProps`, `ProfileBioProps`, `ProfileLinksProps`, `ProfileDateProps`, `ProfileTeamProps`, `ProfileMemberProps`
- `ProfileLinks`: `Profile`, `ProfilePortrait`, `ProfileTeamPhoto`, `ProfileDetails`, `ProfileName`, `ProfileNameLink`, `ProfileRole`, `ProfileBio`, `ProfileLinks`, `ProfileDate`, `ProfileTeam`, `ProfileMember`, `profile`, `ProfileKind`, `ProfileProps`, `ProfilePortraitProps`, `ProfileTeamPhotoProps`, `ProfileDetailsProps`, `ProfileNameProps`, `ProfileNameLinkProps`, `ProfileRoleProps`, `ProfileBioProps`, `ProfileLinksProps`, `ProfileDateProps`, `ProfileTeamProps`, `ProfileMemberProps`
- `ProfileDate`: `Profile`, `ProfilePortrait`, `ProfileTeamPhoto`, `ProfileDetails`, `ProfileName`, `ProfileNameLink`, `ProfileRole`, `ProfileBio`, `ProfileLinks`, `ProfileDate`, `ProfileTeam`, `ProfileMember`, `profile`, `ProfileKind`, `ProfileProps`, `ProfilePortraitProps`, `ProfileTeamPhotoProps`, `ProfileDetailsProps`, `ProfileNameProps`, `ProfileNameLinkProps`, `ProfileRoleProps`, `ProfileBioProps`, `ProfileLinksProps`, `ProfileDateProps`, `ProfileTeamProps`, `ProfileMemberProps`
- `ProfileTeam`: `Profile`, `ProfilePortrait`, `ProfileTeamPhoto`, `ProfileDetails`, `ProfileName`, `ProfileNameLink`, `ProfileRole`, `ProfileBio`, `ProfileLinks`, `ProfileDate`, `ProfileTeam`, `ProfileMember`, `profile`, `ProfileKind`, `ProfileProps`, `ProfilePortraitProps`, `ProfileTeamPhotoProps`, `ProfileDetailsProps`, `ProfileNameProps`, `ProfileNameLinkProps`, `ProfileRoleProps`, `ProfileBioProps`, `ProfileLinksProps`, `ProfileDateProps`, `ProfileTeamProps`, `ProfileMemberProps`
- `ProfileMember`: `Profile`, `ProfilePortrait`, `ProfileTeamPhoto`, `ProfileDetails`, `ProfileName`, `ProfileNameLink`, `ProfileRole`, `ProfileBio`, `ProfileLinks`, `ProfileDate`, `ProfileTeam`, `ProfileMember`, `profile`, `ProfileKind`, `ProfileProps`, `ProfilePortraitProps`, `ProfileTeamPhotoProps`, `ProfileDetailsProps`, `ProfileNameProps`, `ProfileNameLinkProps`, `ProfileRoleProps`, `ProfileBioProps`, `ProfileLinksProps`, `ProfileDateProps`, `ProfileTeamProps`, `ProfileMemberProps`
