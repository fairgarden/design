# Index Rows

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate content/index-rows'

## API Reference

### IndexRow

One row: an optional key column (`rank`, `date` or `thumb`) and the text
column, which holds `IndexRowTitle` (or an event's `IndexRowDisclosure`),
`IndexRowMeta` and `IndexRowDek`. The row rule runs from the text column
to the module edge, never under the key column.

**IndexRow Props:**

| Prop  | Type              | Default | Description                                                                                                                                                      |
| :---- | :---------------- | :------ | :--------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| date  | `React.ReactNode` | -       | Event rows: the date key, e.g. `<time dateTime="2026-07-01">01 / 07</time>`;&#xA;`type-data`. Above the title below 768 px of container, a left column from 768. |
| rank  | `React.ReactNode` | -       | Ranked rows: the rank numeral, with no period ("1", "2" …); `--role-muted`, tabular.                                                                             |
| thumb | `React.ReactNode` | -       | Related rows: a thumbnail `img` (empty alt), `--ds-size-thumb` square, dropped in print.                                                                         |

### IndexRowCount

The ranked label's count, "(29)": `type-body-ui` in `--role-muted`, after the title.

### IndexRowDek

An optional one- or two-line dek under an article title: `type-body-ui` in `--primary12`.

### IndexRowDisclosure

An expandable event row (§10.13): the title is the trigger, inside an
`h3`, with the disclosure glyph; the details (body, "Host" /
"Entrance" pairs, a sm Button) sit in the panel. Collapsed titles are
`--role-muted` and grow to `type-h2` in `--primary12` when open. Rows
print expanded, glyphs hidden.

**IndexRowDisclosure Props:**

| Prop       | Type                                      | Default | Description                                                                          |
| :--------- | :---------------------------------------- | :------ | :----------------------------------------------------------------------------------- |
| meta       | `React.ReactNode`                         | -       | An optional meta line under the title (format, place; "Past"), shown in both states. |
| panelProps | `Omit<CollapsiblePanelProps, 'children'>` | -       | Props for the details panel.                                                         |
| title      | `React.ReactNode`                         | -       | The event title, the trigger's label: `--role-muted` collapsed, `--primary12` open.  |

### IndexRowMeta

The meta line in `--role-muted`: `type-label` caps for article rows,
`type-caption` otherwise. Past rows say "Past" here; never dim them. From
1024 px of container an article row's meta moves to a right-aligned column.

### IndexRows

The index module: an optional `IndexRowsHeader` (the §11.8 eyebrow and
hairline, plus any Tabs), the `IndexRowsList` of `IndexRow`s and an
optional `IndexRowsFooter` ("See all ›"). Rows are separated by rules,
never boxed; numbers and dates use tabular figures.

**IndexRows Props:**

| Prop      | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                                                                                                                                                                                                                                    |
| :-------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| kind\*    | `IndexRowsKind`                                                                                                                                                                                                                                                                                                                                    | -       | Required. `ranked`: a muted rank numeral column, labels with counts,&#xA;hairline rules. `related`: a thumbnail left of each title, hairline&#xA;rules. `event`: a mono date key, `--role-rule` rules and a column rule;&#xA;rows may expand. `article`: serif titles, a caps meta line, optional&#xA;deks, hairline dividers. |
| primary   | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale: titles, keys, rules, bars and glyphs. Never&#xA;defaulted; omitted, it inherits the scope \[D133].                                                                                                                                                                                                        |
| secondary | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale: the title links' underline and the footer&#xA;chevron (`--role-accent`, `--role-glyph`). Never defaulted.                                                                                                                                                                                               |

### IndexRowsFooter

The module footer: a standalone `Link` ("See all ›", `type-label` caps
with its `--role-glyph` chevron). Prints "Full list (short URL)" for lists
of more than 10 rows (§7.6).

### IndexRowsHeader

The module header slot: the §11.8 section header (eyebrow + `--role-hairline`
rule) and, where needed, "Upcoming / Past" Tabs (§9.5). Stays with the
first row in print.

### IndexRowsList

The rows: an `ol` for ranked lists, whose order means something; a `ul` otherwise.

### IndexRowTitle

The row's title: `type-itemhead` for article and event rows,
`type-body-ui` at `--font-weight-6` for ranked and related rows. Wrap an
`IndexRowTitleLink` inside, and follow it with an `IndexRowCount` in
ranked lists.

### IndexRowTitleLink

The row's one link, inside `IndexRowTitle`: a §9.3 title Link whose hit
area stretches over the row. Pass `aria-current="page"` (or `"true"` for
the active filter row) to mark the current row. `index` adds the visited ✓
for long indexes.

**IndexRowTitleLink Props:**

| Prop      | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| :-------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| external  | `boolean \| null`                                                                                                                                                                                                                                                                                                                                  | -       | Adds the arrow-open mark and "(external site)" for assistive technology.&#xA;Default `false`.                                                                                                                                                                                                                                                                                                                                                 |
| index     | `boolean \| null`                                                                                                                                                                                                                                                                                                                                  | -       | Adds the screen-only visited ✓ used in long indexes, such as reference&#xA;and archive lists. Default `false` \[D174, D175].                                                                                                                                                                                                                                                                                                                  |
| list      | `boolean \| null`                                                                                                                                                                                                                                                                                                                                  | -       | With `kind="nav"`: a list link (nav-panel, footer, drawer and breadcrumb&#xA;lists), whose hover is `--role-link-hover` color only, plus the&#xA;`--ds-stroke-1-5` `--role-accent` underline where that ink is&#xA;`--primary12`. Without it, `nav` is bare navigation text (bar and&#xA;utility items, page numbers, toolbar links), whose hover is the&#xA;`--border-size-2` `--role-accent` underline (§9.3) \[D181]. Default&#xA;`false`. |
| muted     | `boolean \| null`                                                                                                                                                                                                                                                                                                                                  | -       | Rests in `--role-muted` instead of `--primary12`, as a breadcrumb's&#xA;ancestors. Hover takes `--role-link-hover` only (the underline is added&#xA;where that ink is `--primary12`); with `kind="nav"` it replaces the bar&#xA;item's bare-text underline (§9.3, §9.8) \[D181]. Default `false`.                                                                                                                                             |
| primary   | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale: the link text and focus ring. Never defaulted;&#xA;omitted, it inherits the scope \[D133].                                                                                                                                                                                                                                                                                                                               |
| secondary | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale: the accent underline. Never defaulted; omitted,&#xA;it inherits the scope.                                                                                                                                                                                                                                                                                                                                             |
| className | `string`                                                                                                                                                                                                                                                                                                                                           | -       | Extra class names, added after the module's own.                                                                                                                                                                                                                                                                                                                                                                                              |

## Additional Types

### IndexRowCountProps

Props for IndexRowCount: `span` props and `render`.

```typescript
type IndexRowCountProps = useRender.ComponentProps<'span'>;
```

### IndexRowDekProps

Props for IndexRowDek: paragraph props and `render`.

```typescript
type IndexRowDekProps = useRender.ComponentProps<'p'>;
```

### IndexRowDisclosureProps

Props for IndexRowDisclosure: Base UI Collapsible Root props plus the title, meta and panel props.

```typescript
type IndexRowDisclosureProps = {
  /** The event title, the trigger's label: `--role-muted` collapsed, `--primary12` open. */
  title: React.ReactNode;
  /** An optional meta line under the title (format, place; "Past"), shown in both states. */
  meta?: React.ReactNode;
  /** Props for the details panel. */
  panelProps?: Omit<CollapsiblePanelProps, 'children'>;
};
```

### IndexRowMetaProps

Props for IndexRowMeta: paragraph props and `render`.

```typescript
type IndexRowMetaProps = useRender.ComponentProps<'p'>;
```

### IndexRowProps

Props for IndexRow: list-item props, `render` and the key column content.

```typescript
type IndexRowProps = React.ClassAttributes<HTMLLIElement> &
  React.LiHTMLAttributes<HTMLLIElement> &
  __type & { rank?: React.ReactNode; date?: React.ReactNode; thumb?: React.ReactNode };
```

### indexRows

```typescript
type indexRows = indexRows;
```

### IndexRowsFooterProps

Props for IndexRowsFooter: `div` props and `render`.

```typescript
type IndexRowsFooterProps = useRender.ComponentProps<'div'>;
```

### IndexRowsHeaderProps

Props for IndexRowsHeader: `header` props and `render`.

```typescript
type IndexRowsHeaderProps = useRender.ComponentProps<'header'>;
```

### IndexRowsKind

The four listing builds (§12.11).

```typescript
type IndexRowsKind = 'ranked' | 'related' | 'event' | 'article';
```

### IndexRowsListProps

Props for IndexRowsList: list props and `render`.

```typescript
type IndexRowsListProps = useRender.ComponentProps<'ul'>;
```

### IndexRowsProps

Props for IndexRows: `section` props, `render`, the kind and the color axes.

```typescript
type IndexRowsProps = React.ClassAttributes<HTMLElement> &
  React.HTMLAttributes<HTMLElement> &
  __type & {
    kind: IndexRowsKind;
    primary?:
      | 'ruby'
      | 'olive'
      | 'sage'
      | 'slate'
      | 'sand'
      | 'gray'
      | 'mauve'
      | 'brown'
      | 'bronze'
      | 'gold'
      | 'red'
      | 'crimson'
      | 'tomato'
      | 'pink'
      | 'plum'
      | 'indigo'
      | 'iris'
      | 'violet'
      | 'purple'
      | null;
    secondary?:
      | 'ruby'
      | 'olive'
      | 'sage'
      | 'slate'
      | 'sand'
      | 'gray'
      | 'mauve'
      | 'brown'
      | 'bronze'
      | 'gold'
      | 'red'
      | 'crimson'
      | 'tomato'
      | 'pink'
      | 'plum'
      | 'indigo'
      | 'iris'
      | 'violet'
      | 'purple'
      | 'amber'
      | 'blue'
      | 'cyan'
      | 'grass'
      | 'green'
      | 'jade'
      | 'lime'
      | 'mint'
      | 'orange'
      | 'sky'
      | 'teal'
      | 'yellow'
      | null;
  };
```

### IndexRowTitleLinkProps

Props for IndexRowTitleLink: Link props except `kind`, which is always `title`.

```typescript
type IndexRowTitleLinkProps = {
  /** Extra class names, added after the module's own. */
  className?: string;
  /**
   * Primary Radix scale: the link text and focus ring. Never defaulted;
   * omitted, it inherits the scope [D133].
   */
  primary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | null;
  /**
   * Secondary Radix scale: the accent underline. Never defaulted; omitted,
   * it inherits the scope.
   */
  secondary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | 'amber'
    | 'blue'
    | 'cyan'
    | 'grass'
    | 'green'
    | 'jade'
    | 'lime'
    | 'mint'
    | 'orange'
    | 'sky'
    | 'teal'
    | 'yellow'
    | null;
  /**
   * With `kind="nav"`: a list link (nav-panel, footer, drawer and breadcrumb
   * lists), whose hover is `--role-link-hover` color only, plus the
   * `--ds-stroke-1-5` `--role-accent` underline where that ink is
   * `--primary12`. Without it, `nav` is bare navigation text (bar and
   * utility items, page numbers, toolbar links), whose hover is the
   * `--border-size-2` `--role-accent` underline (§9.3) [D181]. Default
   * `false`.
   */
  list?: boolean | null;
  /**
   * Adds the screen-only visited ✓ used in long indexes, such as reference
   * and archive lists. Default `false` [D174, D175].
   */
  index?: boolean | null;
  /**
   * Adds the arrow-open mark and "(external site)" for assistive technology.
   * Default `false`.
   */
  external?: boolean | null;
  /**
   * Rests in `--role-muted` instead of `--primary12`, as a breadcrumb's
   * ancestors. Hover takes `--role-link-hover` only (the underline is added
   * where that ink is `--primary12`); with `kind="nav"` it replaces the bar
   * item's bare-text underline (§9.3, §9.8) [D181]. Default `false`.
   */
  muted?: boolean | null;
};
```

### IndexRowTitleProps

Props for IndexRowTitle: heading props and `render` (default `<h3>`).

```typescript
type IndexRowTitleProps = useRender.ComponentProps<'h3'>;
```

## Export Groups

- `IndexRows`: `IndexRows`, `IndexRowsHeader`, `IndexRowsList`, `IndexRow`, `IndexRowTitle`, `IndexRowTitleLink`, `IndexRowCount`, `IndexRowMeta`, `IndexRowDek`, `IndexRowDisclosure`, `IndexRowsFooter`, `indexRows`, `IndexRowsKind`, `IndexRowsProps`, `IndexRowsHeaderProps`, `IndexRowsListProps`, `IndexRowProps`, `IndexRowTitleProps`, `IndexRowTitleLinkProps`, `IndexRowCountProps`, `IndexRowMetaProps`, `IndexRowDekProps`, `IndexRowDisclosureProps`, `IndexRowsFooterProps`
- `IndexRowsHeader`: `IndexRows`, `IndexRowsHeader`, `IndexRowsList`, `IndexRow`, `IndexRowTitle`, `IndexRowTitleLink`, `IndexRowCount`, `IndexRowMeta`, `IndexRowDek`, `IndexRowDisclosure`, `IndexRowsFooter`, `indexRows`, `IndexRowsKind`, `IndexRowsProps`, `IndexRowsHeaderProps`, `IndexRowsListProps`, `IndexRowProps`, `IndexRowTitleProps`, `IndexRowTitleLinkProps`, `IndexRowCountProps`, `IndexRowMetaProps`, `IndexRowDekProps`, `IndexRowDisclosureProps`, `IndexRowsFooterProps`
- `IndexRowsList`: `IndexRows`, `IndexRowsHeader`, `IndexRowsList`, `IndexRow`, `IndexRowTitle`, `IndexRowTitleLink`, `IndexRowCount`, `IndexRowMeta`, `IndexRowDek`, `IndexRowDisclosure`, `IndexRowsFooter`, `indexRows`, `IndexRowsKind`, `IndexRowsProps`, `IndexRowsHeaderProps`, `IndexRowsListProps`, `IndexRowProps`, `IndexRowTitleProps`, `IndexRowTitleLinkProps`, `IndexRowCountProps`, `IndexRowMetaProps`, `IndexRowDekProps`, `IndexRowDisclosureProps`, `IndexRowsFooterProps`
- `IndexRow`: `IndexRows`, `IndexRowsHeader`, `IndexRowsList`, `IndexRow`, `IndexRowTitle`, `IndexRowTitleLink`, `IndexRowCount`, `IndexRowMeta`, `IndexRowDek`, `IndexRowDisclosure`, `IndexRowsFooter`, `indexRows`, `IndexRowsKind`, `IndexRowsProps`, `IndexRowsHeaderProps`, `IndexRowsListProps`, `IndexRowProps`, `IndexRowTitleProps`, `IndexRowTitleLinkProps`, `IndexRowCountProps`, `IndexRowMetaProps`, `IndexRowDekProps`, `IndexRowDisclosureProps`, `IndexRowsFooterProps`
- `IndexRowTitle`: `IndexRows`, `IndexRowsHeader`, `IndexRowsList`, `IndexRow`, `IndexRowTitle`, `IndexRowTitleLink`, `IndexRowCount`, `IndexRowMeta`, `IndexRowDek`, `IndexRowDisclosure`, `IndexRowsFooter`, `indexRows`, `IndexRowsKind`, `IndexRowsProps`, `IndexRowsHeaderProps`, `IndexRowsListProps`, `IndexRowProps`, `IndexRowTitleProps`, `IndexRowTitleLinkProps`, `IndexRowCountProps`, `IndexRowMetaProps`, `IndexRowDekProps`, `IndexRowDisclosureProps`, `IndexRowsFooterProps`
- `IndexRowTitleLink`: `IndexRows`, `IndexRowsHeader`, `IndexRowsList`, `IndexRow`, `IndexRowTitle`, `IndexRowTitleLink`, `IndexRowCount`, `IndexRowMeta`, `IndexRowDek`, `IndexRowDisclosure`, `IndexRowsFooter`, `indexRows`, `IndexRowsKind`, `IndexRowsProps`, `IndexRowsHeaderProps`, `IndexRowsListProps`, `IndexRowProps`, `IndexRowTitleProps`, `IndexRowTitleLinkProps`, `IndexRowCountProps`, `IndexRowMetaProps`, `IndexRowDekProps`, `IndexRowDisclosureProps`, `IndexRowsFooterProps`
- `IndexRowCount`: `IndexRows`, `IndexRowsHeader`, `IndexRowsList`, `IndexRow`, `IndexRowTitle`, `IndexRowTitleLink`, `IndexRowCount`, `IndexRowMeta`, `IndexRowDek`, `IndexRowDisclosure`, `IndexRowsFooter`, `indexRows`, `IndexRowsKind`, `IndexRowsProps`, `IndexRowsHeaderProps`, `IndexRowsListProps`, `IndexRowProps`, `IndexRowTitleProps`, `IndexRowTitleLinkProps`, `IndexRowCountProps`, `IndexRowMetaProps`, `IndexRowDekProps`, `IndexRowDisclosureProps`, `IndexRowsFooterProps`
- `IndexRowMeta`: `IndexRows`, `IndexRowsHeader`, `IndexRowsList`, `IndexRow`, `IndexRowTitle`, `IndexRowTitleLink`, `IndexRowCount`, `IndexRowMeta`, `IndexRowDek`, `IndexRowDisclosure`, `IndexRowsFooter`, `indexRows`, `IndexRowsKind`, `IndexRowsProps`, `IndexRowsHeaderProps`, `IndexRowsListProps`, `IndexRowProps`, `IndexRowTitleProps`, `IndexRowTitleLinkProps`, `IndexRowCountProps`, `IndexRowMetaProps`, `IndexRowDekProps`, `IndexRowDisclosureProps`, `IndexRowsFooterProps`
- `IndexRowDek`: `IndexRows`, `IndexRowsHeader`, `IndexRowsList`, `IndexRow`, `IndexRowTitle`, `IndexRowTitleLink`, `IndexRowCount`, `IndexRowMeta`, `IndexRowDek`, `IndexRowDisclosure`, `IndexRowsFooter`, `indexRows`, `IndexRowsKind`, `IndexRowsProps`, `IndexRowsHeaderProps`, `IndexRowsListProps`, `IndexRowProps`, `IndexRowTitleProps`, `IndexRowTitleLinkProps`, `IndexRowCountProps`, `IndexRowMetaProps`, `IndexRowDekProps`, `IndexRowDisclosureProps`, `IndexRowsFooterProps`
- `IndexRowDisclosure`: `IndexRows`, `IndexRowsHeader`, `IndexRowsList`, `IndexRow`, `IndexRowTitle`, `IndexRowTitleLink`, `IndexRowCount`, `IndexRowMeta`, `IndexRowDek`, `IndexRowDisclosure`, `IndexRowsFooter`, `indexRows`, `IndexRowsKind`, `IndexRowsProps`, `IndexRowsHeaderProps`, `IndexRowsListProps`, `IndexRowProps`, `IndexRowTitleProps`, `IndexRowTitleLinkProps`, `IndexRowCountProps`, `IndexRowMetaProps`, `IndexRowDekProps`, `IndexRowDisclosureProps`, `IndexRowsFooterProps`
- `IndexRowsFooter`: `IndexRows`, `IndexRowsHeader`, `IndexRowsList`, `IndexRow`, `IndexRowTitle`, `IndexRowTitleLink`, `IndexRowCount`, `IndexRowMeta`, `IndexRowDek`, `IndexRowDisclosure`, `IndexRowsFooter`, `indexRows`, `IndexRowsKind`, `IndexRowsProps`, `IndexRowsHeaderProps`, `IndexRowsListProps`, `IndexRowProps`, `IndexRowTitleProps`, `IndexRowTitleLinkProps`, `IndexRowCountProps`, `IndexRowMetaProps`, `IndexRowDekProps`, `IndexRowDisclosureProps`, `IndexRowsFooterProps`
