import { createDemo } from '@/functions/createDemo';
import { CodeBlockVariants } from './CodeBlockVariants';

export const DemoCodeBlockVariants = createDemo(import.meta.url, CodeBlockVariants, {
  name: 'Variants and TS | JS',
  slug: 'variants',
});
