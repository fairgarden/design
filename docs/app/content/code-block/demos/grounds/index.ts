import { createDemo } from '@/functions/createDemo';
import { CodeBlockGrounds } from './CodeBlockGrounds';

export const DemoCodeBlockGrounds = createDemo(import.meta.url, CodeBlockGrounds, {
  name: 'On grounds',
  slug: 'grounds',
});
