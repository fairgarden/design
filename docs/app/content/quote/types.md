# Quote

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate content/quote'

## API Reference

### Quote

The quote: a `figure` holding `QuoteText` (the `blockquote`) and, for
testimonials, a `QuoteAttribution` (`figcaption`) with `QuoteName`,
`QuoteRole`, an optional `QuotePortrait` and an optional `QuoteSource`.
Keep band and framed testimonials to 40 words; never put quotes in a
carousel.

**Quote Props:**

| Prop      | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                                                                                                                                  |
| :-------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| duplicate | `boolean`                                                                                                                                                                                                                                                                                                                                          | -       | The pull quote repeats text from the article: it renders as an&#xA;`aside` hidden from assistive technology and is hidden in print&#xA;\[D174]. Default `false`: a pull quote with unique text is a `figure`&#xA;and prints. |
| kind\*    | `'pull' \| 'band' \| 'framed' \| 'block' \| 'epigraph'`                                                                                                                                                                                                                                                                                            | -       | A pull quote: `type-quote` followed by a short rule, no attribution.                                                                                                                                                         |
| primary   | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale: text, mark, rules, frame and attribution. Never&#xA;defaulted; omitted, it inherits the scope \[D133].                                                                                                  |
| secondary | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale: only a linked source's underline&#xA;(`--role-accent`). Never defaulted.                                                                                                                              |

### QuoteAttribution

Who said it, `--size-px-5` below the quote: `QuotePortrait`, then
`QuoteName` over `QuoteRole` (a line break, never a pipe), then an
optional `QuoteSource`.

### QuoteName

The person's name: `type-label` caps in `--primary12`, on its own line.

### QuotePortrait

The portrait: an Avatar at 40 px (`md`) with its `--role-rule` ring,
taking the quote's `primary` and `secondary` unless given its own. Compose
`AvatarImage` (empty alt, since the name is adjacent) and
`AvatarFallback` inside. Prints grayscale, at most 20 mm.

**QuotePortrait Props:**

| Prop      | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                                                                                                                   |
| :-------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| primary   | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale: the ring. Never defaulted; omitted, it inherits&#xA;the scope \[D133].                                                                                                                   |
| secondary | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale, accepted for the shared contract; no part uses it.                                                                                                                                     |
| size      | `'xs' \| 'sm' \| 'md' \| 'lg' \| 'xl' \| null`                                                                                                                                                                                                                                                                                                     | -       | `xs` 24, `sm` 32, `md` 40 (default), `lg` 64, `xl` 128 px. Up to 40 px&#xA;the ring is `--border-size-1` `--role-rule`; from 64 px it is&#xA;`--border-size-2` `--primary12`. Sizes are fixed at every width. |

### QuoteRole

Role or affiliation under the name: `type-caption` in `--primary12`.

### QuoteSource

The source title in a `cite` (`type-caption`, upright). Link it with a
`Link` inside, which owns the `--role-accent` underline and its states.

### QuoteText

The quoted words, a `blockquote` (pass `cite` for a source URL):
`type-quote` for band, framed and pull; `type-body` behind the left rule
for block; display bold italic for epigraph. Curly quotes only; pull
quotes hang their punctuation. A pull quote closes with its short rule.

## Additional Types

### quote

```typescript
type quote = quote;
```

### QuoteAttributionProps

Props for QuoteAttribution: `figcaption` props and `render`.

```typescript
type QuoteAttributionProps = useRender.ComponentProps<'figcaption'>;
```

### QuoteKind

The five quote builds (§12.8).

```typescript
type QuoteKind = 'band' | 'framed' | 'pull' | 'block' | 'epigraph';
```

### QuoteNameProps

Props for QuoteName: `span` props and `render`.

```typescript
type QuoteNameProps = useRender.ComponentProps<'span'>;
```

### QuotePortraitProps

Props for QuotePortrait: Avatar props; `size` defaults to `md`.

```typescript
type QuotePortraitProps = AvatarProps;
```

### QuoteProps

Props for Quote: `figure` props, `render`, the kind (and `duplicate`) and the color axes.

```typescript
type QuoteProps =
  | (React.ClassAttributes<HTMLElement> &
      React.HTMLAttributes<HTMLElement> &
      __type & { kind: 'pull'; duplicate?: boolean } & {
        primary?:
          | 'ruby'
          | 'olive'
          | 'sage'
          | 'slate'
          | 'sand'
          | 'gray'
          | 'mauve'
          | 'brown'
          | 'bronze'
          | 'gold'
          | 'red'
          | 'crimson'
          | 'tomato'
          | 'pink'
          | 'plum'
          | 'indigo'
          | 'iris'
          | 'violet'
          | 'purple'
          | null;
        secondary?:
          | 'ruby'
          | 'olive'
          | 'sage'
          | 'slate'
          | 'sand'
          | 'gray'
          | 'mauve'
          | 'brown'
          | 'bronze'
          | 'gold'
          | 'red'
          | 'crimson'
          | 'tomato'
          | 'pink'
          | 'plum'
          | 'indigo'
          | 'iris'
          | 'violet'
          | 'purple'
          | 'amber'
          | 'blue'
          | 'cyan'
          | 'grass'
          | 'green'
          | 'jade'
          | 'lime'
          | 'mint'
          | 'orange'
          | 'sky'
          | 'teal'
          | 'yellow'
          | null;
      })
  | (React.ClassAttributes<HTMLElement> &
      React.HTMLAttributes<HTMLElement> &
      __type & { kind: 'band' | 'framed' | 'block' | 'epigraph'; duplicate?: undefined } & {
        primary?:
          | 'ruby'
          | 'olive'
          | 'sage'
          | 'slate'
          | 'sand'
          | 'gray'
          | 'mauve'
          | 'brown'
          | 'bronze'
          | 'gold'
          | 'red'
          | 'crimson'
          | 'tomato'
          | 'pink'
          | 'plum'
          | 'indigo'
          | 'iris'
          | 'violet'
          | 'purple'
          | null;
        secondary?:
          | 'ruby'
          | 'olive'
          | 'sage'
          | 'slate'
          | 'sand'
          | 'gray'
          | 'mauve'
          | 'brown'
          | 'bronze'
          | 'gold'
          | 'red'
          | 'crimson'
          | 'tomato'
          | 'pink'
          | 'plum'
          | 'indigo'
          | 'iris'
          | 'violet'
          | 'purple'
          | 'amber'
          | 'blue'
          | 'cyan'
          | 'grass'
          | 'green'
          | 'jade'
          | 'lime'
          | 'mint'
          | 'orange'
          | 'sky'
          | 'teal'
          | 'yellow'
          | null;
      });
```

### QuoteRoleProps

Props for QuoteRole: `span` props and `render`.

```typescript
type QuoteRoleProps = useRender.ComponentProps<'span'>;
```

### QuoteSourceProps

Props for QuoteSource: `cite` props and `render`.

```typescript
type QuoteSourceProps = useRender.ComponentProps<'cite'>;
```

### QuoteTextProps

Props for QuoteText: `blockquote` props and `render`.

```typescript
type QuoteTextProps = useRender.ComponentProps<'blockquote'>;
```

## Export Groups

- `Quote`: `Quote`, `QuoteText`, `QuoteAttribution`, `QuoteName`, `QuoteRole`, `QuotePortrait`, `QuoteSource`, `quote`, `QuoteKind`, `QuoteProps`, `QuoteTextProps`, `QuoteAttributionProps`, `QuoteNameProps`, `QuoteRoleProps`, `QuotePortraitProps`, `QuoteSourceProps`
- `QuoteText`: `Quote`, `QuoteText`, `QuoteAttribution`, `QuoteName`, `QuoteRole`, `QuotePortrait`, `QuoteSource`, `quote`, `QuoteKind`, `QuoteProps`, `QuoteTextProps`, `QuoteAttributionProps`, `QuoteNameProps`, `QuoteRoleProps`, `QuotePortraitProps`, `QuoteSourceProps`
- `QuoteAttribution`: `Quote`, `QuoteText`, `QuoteAttribution`, `QuoteName`, `QuoteRole`, `QuotePortrait`, `QuoteSource`, `quote`, `QuoteKind`, `QuoteProps`, `QuoteTextProps`, `QuoteAttributionProps`, `QuoteNameProps`, `QuoteRoleProps`, `QuotePortraitProps`, `QuoteSourceProps`
- `QuoteName`: `Quote`, `QuoteText`, `QuoteAttribution`, `QuoteName`, `QuoteRole`, `QuotePortrait`, `QuoteSource`, `quote`, `QuoteKind`, `QuoteProps`, `QuoteTextProps`, `QuoteAttributionProps`, `QuoteNameProps`, `QuoteRoleProps`, `QuotePortraitProps`, `QuoteSourceProps`
- `QuoteRole`: `Quote`, `QuoteText`, `QuoteAttribution`, `QuoteName`, `QuoteRole`, `QuotePortrait`, `QuoteSource`, `quote`, `QuoteKind`, `QuoteProps`, `QuoteTextProps`, `QuoteAttributionProps`, `QuoteNameProps`, `QuoteRoleProps`, `QuotePortraitProps`, `QuoteSourceProps`
- `QuotePortrait`: `Quote`, `QuoteText`, `QuoteAttribution`, `QuoteName`, `QuoteRole`, `QuotePortrait`, `QuoteSource`, `quote`, `QuoteKind`, `QuoteProps`, `QuoteTextProps`, `QuoteAttributionProps`, `QuoteNameProps`, `QuoteRoleProps`, `QuotePortraitProps`, `QuoteSourceProps`
- `QuoteSource`: `Quote`, `QuoteText`, `QuoteAttribution`, `QuoteName`, `QuoteRole`, `QuotePortrait`, `QuoteSource`, `quote`, `QuoteKind`, `QuoteProps`, `QuoteTextProps`, `QuoteAttributionProps`, `QuoteNameProps`, `QuoteRoleProps`, `QuotePortraitProps`, `QuoteSourceProps`
