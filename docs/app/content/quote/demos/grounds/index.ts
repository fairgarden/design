import { createDemo } from '@/functions/createDemo';
import { QuoteGrounds } from './QuoteGrounds';

export const DemoQuoteGrounds = createDemo(import.meta.url, QuoteGrounds, {
  name: 'On paper, forest and clay',
  slug: 'grounds',
});
