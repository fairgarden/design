import { createDemo } from '@/functions/createDemo';
import { SpecSheetGlance } from './SpecSheetGlance';

export const DemoSpecSheetGlance = createDemo(import.meta.url, SpecSheetGlance, {
  name: 'At a Glance and the split',
  slug: 'glance',
});
