import { createDemo } from '@/functions/createDemo';
import { ScrollAreaKinds } from './ScrollAreaKinds';

export const DemoScrollAreaKinds = createDemo(import.meta.url, ScrollAreaKinds, {
  name: 'Panel, wide and rail',
  slug: 'kinds',
});
