import { createDemo } from '@/functions/createDemo';
import { SpecGridGlance } from './SpecGridGlance';

export const DemoSpecGridGlance = createDemo(import.meta.url, SpecGridGlance, {
  name: 'At a glance',
  slug: 'glance',
});
