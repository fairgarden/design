import { createDemo } from '@/functions/createDemo';
import { CollapsibleGrounds } from './CollapsibleGrounds';

export const DemoCollapsibleGrounds = createDemo(import.meta.url, CollapsibleGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
