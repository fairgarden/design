import { createDemo } from '@/functions/createDemo';
import { CollapsibleColor } from './CollapsibleColor';

export const DemoCollapsibleColor = createDemo(import.meta.url, CollapsibleColor, {
  name: 'Primary scale',
  slug: 'color',
});
