import { createDemo } from '@/functions/createDemo';
import { CollapsibleVariants } from './CollapsibleVariants';

export const DemoCollapsibleVariants = createDemo(import.meta.url, CollapsibleVariants, {
  name: 'Show more',
  slug: 'variants',
});
