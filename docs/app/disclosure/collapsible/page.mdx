# Collapsible

A Base UI Collapsible for one in-place disclosure, such as "Show 12 more" under a truncated list, led by the disclosure glyph.

```tsx
import {
  Collapsible,
  CollapsiblePanel,
  CollapsibleTrigger,
} from '@fairgarden/design/disclosure/collapsible'
```

The trigger is the disclosure glyph at the 16 px tier plus a label, and the whole trigger is the target. `openLabel` swaps the label while the panel is open ("Show less"). The panel opens with a clip reveal, instantly under reduced motion, and stays in the DOM, so it prints expanded while the trigger is hidden. `DisclosureGlyph` is exported for any other in-place disclosure; it reads its state from the Base UI trigger around it.

## Show more

import { DemoCollapsibleVariants } from './demos/variants'

<DemoCollapsibleVariants.Title />

The arrows point outward while collapsed and inward while open, muted then in the text ink; they never rotate. A disabled trigger mutes label and glyph.

<DemoCollapsibleVariants />

[See Demo](./demos/variants/)

## Primary scale

import { DemoCollapsibleColor } from './demos/color'

<DemoCollapsibleColor.Title />

<DemoCollapsibleColor />

[See Demo](./demos/color/)

## On grounds

import { DemoCollapsibleGrounds } from './demos/grounds'

<DemoCollapsibleGrounds.Title />

<DemoCollapsibleGrounds />

[See Demo](./demos/grounds/)

## API Reference

import { TypesCollapsible, TypesCollapsibleAdditional } from './types'

### Collapsible

<TypesCollapsible.Collapsible />

### CollapsibleTrigger and CollapsiblePanel

<TypesCollapsible.CollapsibleTrigger />

<TypesCollapsible.CollapsiblePanel />

### DisclosureGlyph

<TypesCollapsible.DisclosureGlyph />

### Additional types

<TypesCollapsibleAdditional />

[See Types](./types.md)

Specification: `DESIGN-SYSTEM.md` §10.13 (accordion and collapsible) and §6.10 (the disclosure glyph).
