# Meter

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate feedback/meter'

## API Reference

### Meter

A Base UI Meter drawn in ink only: every part is `--primary12` on the
ground; the value is always in text. In print, linear and segmented
indicators turn to a 45° hatch inside their outline, closed by an edge at
the value; rings and ordinal scales print as line.

**Meter Props:**

| Prop        | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                                                                                                                                                                                                   |
| :---------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| label       | `React.ReactNode`                                                                                                                                                                                                                                                                                                                                  | -       | The label, in caps (`type-label`); author it in sentence case.                                                                                                                                                                                                                                |
| value       | `number`                                                                                                                                                                                                                                                                                                                                           | -       | The measured value, between `min` and `max`; for `ordinal`, the index of the active stop.                                                                                                                                                                                                     |
| formatValue | `((formattedValue: string, value: number) => React.ReactNode)`                                                                                                                                                                                                                                                                                     | -       | Formats the visible value (`type-data`): "62%", "4.2 of 10 GB". Default: the Base UI formatted value.                                                                                                                                                                                         |
| kind        | `'bar' \| 'steps' \| 'ordinal' \| 'ring' \| null`                                                                                                                                                                                                                                                                                                  | -       | `bar` (default): an outlined 8 px track with a solid indicator. `steps`:&#xA;one outlined 12 px cell per unit (`max` − `min` cells). `ordinal`: a&#xA;1 px track with a tick and a caps label per stop, the active stop bold,&#xA;larger and pointed to. `ring`: a 2 px arc beside its value. |
| primary     | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale: track, indicator, ticks, stops and text. Never&#xA;defaulted; omitted, it inherits the scope \[D133].                                                                                                                                                                    |
| secondary   | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale: unused, so a meter never reads as a chart series. Never defaulted.                                                                                                                                                                                                     |
| status      | `MeterStatus`                                                                                                                                                                                                                                                                                                                                      | -       | The status beside the value; always with `statusText`.                                                                                                                                                                                                                                        |
| statusText  | `React.ReactNode`                                                                                                                                                                                                                                                                                                                                  | -       | The status word: "Near limit", "Over limit". In a form it is also the Field description.                                                                                                                                                                                                      |
| stops       | `React.ReactNode[]`                                                                                                                                                                                                                                                                                                                                | -       | `ordinal` only: the stop labels in order (author them in sentence case;&#xA;CSS sets the caps). `min` becomes 0 and `max` the last index.                                                                                                                                                     |
| threshold   | `number`                                                                                                                                                                                                                                                                                                                                           | -       | A limit, drawn as a --size-px-2-5 tick at --border-size-2 across the track (`bar`).                                                                                                                                                                                                           |

### MeterPanel

A meter or scale panel: stacks its meters and is the `meter-panel`
container, so each meter sets label | track | value in one row from
768 px of the panel's width, and stacks below it.

## Additional Types

### meter

```typescript
type meter = meter;
```

### MeterPanelProps

Props for MeterPanel: `div` props.

```typescript
type MeterPanelProps = React.ComponentPropsWithRef<'div'>;
```

### MeterProps

Props for Meter: Base UI Meter Root props plus the kind and color axes, the label, stops, threshold and status.

```typescript
type MeterProps = {
  /** The measured value, between `min` and `max`; for `ordinal`, the index of the active stop. */
  value: number;
  /**
   * `bar` (default): an outlined 8 px track with a solid indicator. `steps`:
   * one outlined 12 px cell per unit (`max` − `min` cells). `ordinal`: a
   * 1 px track with a tick and a caps label per stop, the active stop bold,
   * larger and pointed to. `ring`: a 2 px arc beside its value.
   */
  kind?: 'bar' | 'steps' | 'ordinal' | 'ring' | null;
  /**
   * Primary Radix scale: track, indicator, ticks, stops and text. Never
   * defaulted; omitted, it inherits the scope [D133].
   */
  primary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | null;
  /** Secondary Radix scale: unused, so a meter never reads as a chart series. Never defaulted. */
  secondary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | 'amber'
    | 'blue'
    | 'cyan'
    | 'grass'
    | 'green'
    | 'jade'
    | 'lime'
    | 'mint'
    | 'orange'
    | 'sky'
    | 'teal'
    | 'yellow'
    | null;
  /** The label, in caps (`type-label`); author it in sentence case. */
  label?: React.ReactNode;
  /** Formats the visible value (`type-data`): "62%", "4.2 of 10 GB". Default: the Base UI formatted value. */
  formatValue?: (formattedValue: string, value: number) => React.ReactNode;
  /**
   * `ordinal` only: the stop labels in order (author them in sentence case;
   * CSS sets the caps). `min` becomes 0 and `max` the last index.
   */
  stops?: React.ReactNode[];
  /** A limit, drawn as a --size-px-2-5 tick at --border-size-2 across the track (`bar`). */
  threshold?: number;
  /** The status beside the value; always with `statusText`. */
  status?: MeterStatus;
  /** The status word: "Near limit", "Over limit". In a form it is also the Field description. */
  statusText?: React.ReactNode;
};
```

### meterStatus

The status part: a §1.5.4 glyph plus a word, in the status scale \[D129].

```typescript
type meterStatus = meterStatus;
```

### MeterStatus

A status beside the value: `warning` ▲ "Near limit", `danger` ◆ "Over limit", `success` ●.

```typescript
type MeterStatus = 'success' | 'warning' | 'danger';
```

## Export Groups

- `Meter`: `Meter`, `MeterPanel`, `meter`, `meterStatus`, `MeterStatus`, `MeterProps`, `MeterPanelProps`
- `MeterPanel`: `Meter`, `MeterPanel`, `meter`, `meterStatus`, `MeterStatus`, `MeterProps`, `MeterPanelProps`
