import { createDemo } from '@/functions/createDemo';
import { AlertGrounds } from './AlertGrounds';

export const DemoAlertGrounds = createDemo(import.meta.url, AlertGrounds, {
  name: 'On paper, forest and leaf',
  slug: 'grounds',
});
