import { createDemo } from '@/functions/createDemo';
import { BadgeGrounds } from './BadgeGrounds';

export const DemoBadgeGrounds = createDemo(import.meta.url, BadgeGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
