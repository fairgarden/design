import { createTypes } from '@/functions/createTypes';
import { Pagination } from '@fairgarden/design/navigation/pagination';

export const TypesPagination = createTypes(import.meta.url, Pagination);
