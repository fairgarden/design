import { createDemo } from '@/functions/createDemo';
import { ToolbarGrounds } from './ToolbarGrounds';

export const DemoToolbarGrounds = createDemo(import.meta.url, ToolbarGrounds, {
  name: 'On page grounds and a deep field',
  slug: 'grounds',
});
