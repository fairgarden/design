import { createDemo } from '@/functions/createDemo';
import { TabsGrounds } from './TabsGrounds';

export const DemoTabsGrounds = createDemo(import.meta.url, TabsGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
