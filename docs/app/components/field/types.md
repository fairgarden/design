# Field

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate components/field'

## API Reference

### Field

A Base UI Field.Root: the stack of label, control, description and error.
Validation follows §10.2: validate on submit, then on blur for a field
that has already failed (`validationMode` on the Form).

**Field Props:**

| Prop      | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                                              |
| :-------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :--------------------------------------------------------------------------------------------------------------------------------------- |
| primary   | `'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'ruby' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale: the label, description and, by inheritance, the&#xA;control's edge, value and focus ring. Never defaulted \[D133].  |
| secondary | `'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'ruby' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale. Unused at rest; while the field is invalid the&#xA;danger scale replaces it on the error and the control (§10.1). |

### FieldDescription

Helper text: `type-caption` in `--role-muted`, `--size-px-1` below the
box; sentence case. Base UI links it to the control with
`aria-describedby`.

### FieldError

The error message, shown by Base UI while the field is invalid (or for
the `match` it names): the danger glyph in `--role-status`, then the
message in `type-caption` at `--font-weight-6` in `--primary12`. Word it
as what happened plus how to fix it (§1.9.2). Without children it shows
the browser's validation message.

### FieldLabel

The field label: `type-field-label` in `--primary12`, authored in title
case up to about four words, sentence case for longer or question labels
\[D160]. For a Select or Combobox trigger pass `nativeLabel={false}`: the
label then renders a `div` that focuses the control without opening it.

**FieldLabel Props:**

| Prop     | Type                | Default | Description                                                                                                                                                                                         |
| :------- | :------------------ | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| optional | `boolean \| string` | -       | Appends the marker inside the label (§10.1): `true` writes&#xA;"(optional)"; a string writes that marker instead, such as&#xA;"(required)" where most fields are optional. Never an asterisk alone. |

### useFieldInvalid

True while the nearest Field is invalid. Text-entry and select controls
read it to take the danger scale as their `secondary` (§10.1) \[D129].

**useFieldInvalid Return Value:**

```tsx
type ReturnValue = boolean;
```

## Additional Types

### dangerScale

The §1.5.4 danger scale, passed as `secondary` while a field is invalid \[D129].

```typescript
type dangerScale = 'red';
```

### field

```typescript
type field = field;
```

### FieldDescriptionProps

Props for FieldDescription: Base UI Field.Description props.

```typescript
type FieldDescriptionProps = FieldDescription.FieldDescriptionProps;
```

### FieldErrorProps

Props for FieldError: Base UI Field.Error props, without `render`.

```typescript
type FieldErrorProps = Omit<FieldError.FieldErrorProps, 'render'>;
```

### FieldLabelProps

Props for FieldLabel: Base UI Field.Label props plus the "(optional)" marker.

```typescript
type FieldLabelProps = FieldLabelProps & { optional?: boolean | string };
```

### FieldProps

Props for Field: Base UI Field.Root props plus the color axes.

```typescript
type FieldProps = FieldRootProps & {
  primary?:
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'ruby'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | null;
  secondary?:
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'ruby'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | 'amber'
    | 'blue'
    | 'cyan'
    | 'grass'
    | 'green'
    | 'jade'
    | 'lime'
    | 'mint'
    | 'orange'
    | 'sky'
    | 'teal'
    | 'yellow'
    | null;
};
```

## Export Groups

- `Field`: `useFieldInvalid`, `Field`, `FieldLabel`, `FieldDescription`, `FieldError`, `field`, `dangerScale`, `FieldProps`, `FieldLabelProps`, `FieldDescriptionProps`, `FieldErrorProps`
- `FieldLabel`: `useFieldInvalid`, `Field`, `FieldLabel`, `FieldDescription`, `FieldError`, `field`, `dangerScale`, `FieldProps`, `FieldLabelProps`, `FieldDescriptionProps`, `FieldErrorProps`
- `FieldDescription`: `useFieldInvalid`, `Field`, `FieldLabel`, `FieldDescription`, `FieldError`, `field`, `dangerScale`, `FieldProps`, `FieldLabelProps`, `FieldDescriptionProps`, `FieldErrorProps`
- `FieldError`: `useFieldInvalid`, `Field`, `FieldLabel`, `FieldDescription`, `FieldError`, `field`, `dangerScale`, `FieldProps`, `FieldLabelProps`, `FieldDescriptionProps`, `FieldErrorProps`
