import { createDemo } from '@/functions/createDemo';
import { IndexRowsKinds } from './IndexRowsKinds';

export const DemoIndexRowsKinds = createDemo(import.meta.url, IndexRowsKinds, {
  name: 'Ranked, related and article',
  slug: 'kinds',
});
