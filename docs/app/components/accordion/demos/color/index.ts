import { createDemo } from '@/functions/createDemo';
import { AccordionColor } from './AccordionColor';

export const DemoAccordionColor = createDemo(import.meta.url, AccordionColor, {
  name: 'Primary scale',
  slug: 'color',
});
