import { createDemo } from '@/functions/createDemo';
import { AccordionGrounds } from './AccordionGrounds';

export const DemoAccordionGrounds = createDemo(import.meta.url, AccordionGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
