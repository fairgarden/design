import { createDemo } from '@/functions/createDemo';
import { AccordionVariants } from './AccordionVariants';

export const DemoAccordionVariants = createDemo(import.meta.url, AccordionVariants, {
  name: 'Glyphs and the strong top',
  slug: 'variants',
});
