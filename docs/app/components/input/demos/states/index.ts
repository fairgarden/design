import { createDemo } from '@/functions/createDemo';
import { InputStates } from './InputStates';

export const DemoInputStates = createDemo(import.meta.url, InputStates, {
  name: 'Variants and states',
  slug: 'states',
});
