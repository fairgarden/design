import { createDemo } from '@/functions/createDemo';
import { IconGrounds } from './IconGrounds';

export const DemoIconGrounds = createDemo(import.meta.url, IconGrounds, {
  name: 'On paper, forest and leaf',
  slug: 'grounds',
});
