import { createDemo } from '@/functions/createDemo';
import { CardGrounds } from './CardGrounds';

export const DemoCardGrounds = createDemo(import.meta.url, CardGrounds, {
  name: 'On paper, forest and leaf',
  slug: 'grounds',
});
