import { createDemo } from '@/functions/createDemo';
import { SeparatorColor } from './SeparatorColor';

export const DemoSeparatorColor = createDemo(import.meta.url, SeparatorColor, {
  name: 'Primary scale',
  slug: 'color',
});
