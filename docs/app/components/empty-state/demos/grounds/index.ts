import { createDemo } from '@/functions/createDemo';
import { EmptyStateGrounds } from './EmptyStateGrounds';

export const DemoEmptyStateGrounds = createDemo(import.meta.url, EmptyStateGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
