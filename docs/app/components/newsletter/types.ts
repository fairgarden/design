import { createTypes } from '@/functions/createTypes';
import { Newsletter } from '@fairgarden-private/design/components/Newsletter';

export const TypesNewsletter = createTypes(import.meta.url, Newsletter);
