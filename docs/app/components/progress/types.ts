import { createTypes } from '@/functions/createTypes';
import { Progress } from '@fairgarden-private/design/components/Progress';

export const TypesProgress = createTypes(import.meta.url, Progress);
