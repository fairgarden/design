import { createDemo } from '@/functions/createDemo';
import { SliderStates } from './SliderStates';

export const DemoSliderStates = createDemo(import.meta.url, SliderStates, {
  name: 'States',
  slug: 'states',
});
