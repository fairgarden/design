import { createDemo } from '@/functions/createDemo';
import { ToggleGrounds } from './ToggleGrounds';

export const DemoToggleGrounds = createDemo(import.meta.url, ToggleGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
