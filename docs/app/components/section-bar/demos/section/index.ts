import { createDemo } from '@/functions/createDemo';
import { SectionBarSection } from './SectionBarSection';

export const DemoSectionBarSection = createDemo(import.meta.url, SectionBarSection, {
  name: 'Section bar',
  slug: 'section',
});
