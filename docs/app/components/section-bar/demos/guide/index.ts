import { createDemo } from '@/functions/createDemo';
import { SectionBarGuide } from './SectionBarGuide';

export const DemoSectionBarGuide = createDemo(import.meta.url, SectionBarGuide, {
  name: 'Guide bar',
  slug: 'guide',
});
