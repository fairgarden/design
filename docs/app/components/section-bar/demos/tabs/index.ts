import { createDemo } from '@/functions/createDemo';
import { SectionBarTabs } from './SectionBarTabs';

export const DemoSectionBarTabs = createDemo(import.meta.url, SectionBarTabs, {
  name: 'Tab strip',
  slug: 'tabs',
});
