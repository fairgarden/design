import { createDemo } from '@/functions/createDemo';
import { TagGrounds } from './TagGrounds';

export const DemoTagGrounds = createDemo(import.meta.url, TagGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
