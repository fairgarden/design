import { createTypes } from '@/functions/createTypes';
import { NumberField } from '@fairgarden-private/design/components/NumberField';

export const TypesNumberField = createTypes(import.meta.url, NumberField);
