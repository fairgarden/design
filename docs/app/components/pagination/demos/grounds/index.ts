import { createDemo } from '@/functions/createDemo';
import { PaginationGrounds } from './PaginationGrounds';

export const DemoPaginationGrounds = createDemo(import.meta.url, PaginationGrounds, {
  name: 'On page grounds and a deep field',
  slug: 'grounds',
});
