import { createDemo } from '@/functions/createDemo';
import { TimelineTrail } from './TimelineTrail';

export const DemoTimelineTrail = createDemo(import.meta.url, TimelineTrail, {
  name: 'Trail and date block',
  slug: 'trail',
});
