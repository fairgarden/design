# Navigation Bar

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate components/navigation-bar'

## API Reference

### NavigationBar

The site header: a skip link first, the green brand strip, an optional
utility row, then the bar holding the logo (a home link), the Navigation
Menu (from --ds-nav-inline-n-above), icon Buttons, the one action and,
below the threshold, the drawer's menu Button. It is opaque, ruled at the
bottom in --role-rule, 56 px tall (64 px from --lg-n-above), and its
items never wrap: overflow goes to the utility row, then a "More" item
\[D183]. It prints as the §7.8 masthead's logo only.

**NavigationBar Props:**

| Prop         | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                                                                                                 |
| :----------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| action       | `React.ReactNode`                                                                                                                                                                                                                                                                                                                                  | -       | The one primary action: a `solid` Button at `size="sm"`. Below 480 px it moves to the drawer footer, so repeat it there.                                                                    |
| brandDrawing | `React.ReactNode`                                                                                                                                                                                                                                                                                                                                  | -       | An optional fine-line brand drawing (--ds-stroke-0-75) beside the logo; dropped when compact.                                                                                               |
| compact      | `boolean \| null`                                                                                                                                                                                                                                                                                                                                  | -       | `true`: the compact header, even undocked. The sticky observer sets it while docked. Default `false`.                                                                                       |
| currentPath  | `string`                                                                                                                                                                                                                                                                                                                                           | -       | The current page's URL path, shared with the menu, drawer and utility links.                                                                                                                |
| dock         | `NavigationBarDock`                                                                                                                                                                                                                                                                                                                                | -       | How the header docks. Default `none`.                                                                                                                                                       |
| drawer       | `React.ReactNode`                                                                                                                                                                                                                                                                                                                                  | -       | The `NavDrawer` (menu Button and sheet), shown below --ds-nav-inline-n-above.                                                                                                               |
| logo         | `React.ReactNode`                                                                                                                                                                                                                                                                                                                                  | -       | The logo: an ink logo in `currentColor` (an `svg` or `img`), 28 px tall, 36 px from --lg-n-above.                                                                                           |
| logoHref     | `string`                                                                                                                                                                                                                                                                                                                                           | -       | The home link. Default "/".                                                                                                                                                                 |
| logoLabel    | `string`                                                                                                                                                                                                                                                                                                                                           | -       | The home link's accessible name, e.g. "FairGarden home".                                                                                                                                    |
| masthead     | `boolean \| null`                                                                                                                                                                                                                                                                                                                                  | -       | `true`: the stacked masthead. Default `false`.                                                                                                                                              |
| onDockChange | `((docked: boolean) => void)`                                                                                                                                                                                                                                                                                                                      | -       | Called when the header docks (`true`) or returns to its place (`false`).                                                                                                                    |
| preset       | `BandPreset`                                                                                                                                                                                                                                                                                                                                       | -       | The header preset: a page ground (`paper`, `white` or the page's&#xA;pastel), or `night` only directly over the night media hero. Omitted,&#xA;the header takes the page ground it sits in. |
| primary      | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Override the preset's primary (a pairing §2 verifies). Never defaulted \[D133].                                                                                                             |
| ruled        | `boolean \| null`                                                                                                                                                                                                                                                                                                                                  | -       | `true`: vertical --border-size-1 rules between items. Default `false`.                                                                                                                      |
| search       | `React.ReactNode`                                                                                                                                                                                                                                                                                                                                  | -       | Icon Buttons, e.g. `<Search kind="trigger" … />`, `--size-px-1` apart.                                                                                                                      |
| secondary    | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Override the preset's secondary. Never defaulted.                                                                                                                                           |
| sentinelRef  | `React.RefObject<Element \| null>`                                                                                                                                                                                                                                                                                                                 | -       | With `dock="swap"`: a zero-height element at the section bar's in-flow position.                                                                                                            |
| skipHref     | `string`                                                                                                                                                                                                                                                                                                                                           | -       | The skip link's target. Default "#main".                                                                                                                                                    |
| skipLabel    | `string`                                                                                                                                                                                                                                                                                                                                           | -       | The skip link's words. Default "Skip to main content".                                                                                                                                      |
| utility      | `React.ReactNode`                                                                                                                                                                                                                                                                                                                                  | -       | A `NavigationBarUtility` row above the bar, from --ds-nav-inline-n-above; never docks.                                                                                                      |
| children     | `React.ReactNode`                                                                                                                                                                                                                                                                                                                                  | -       | The `NavigationMenu`, inline from --ds-nav-inline-n-above.                                                                                                                                  |

### NavigationBarUtility

The 36 px utility row above the bar (§11.3): secondary destinations as
text links in a `nav` labelled "Utility", divided from the bar by a
\--role-hairline rule, aligned to --ds-container-content. Shown from
\--ds-nav-inline-n-above (below it, repeat its links in the drawer
footer); the compact docked header drops it \[D183]. Put it in the bar's
`utility` slot.

**NavigationBarUtility Props:**

| Prop     | Type              | Default | Description                                                                                  |
| :------- | :---------------- | :------ | :------------------------------------------------------------------------------------------- |
| label    | `string`          | -       | The landmark's name. Default "Utility".                                                      |
| locale   | `React.ReactNode` | -       | An optional locale link at the end ("English ▾ Change language"); a control never sits here. |
| start    | `React.ReactNode` | -       | An optional start slot: a phone number or tagline, in --primary12.                           |
| children | `React.ReactNode` | -       | The row's `NavigationBarUtilityLink`s: text links only.                                      |

### NavigationBarUtilityLink

A utility link: `type-label` caps, no rest underline, the bare-text
underline on hover \[D181], a 24 px target at a 24 px pitch (the §1.5.12
utility exemption). The current page (its path equals the bar's
`currentPath`) takes `aria-current="page"` and the --ds-stroke-3 bar.

## Additional Types

### navigationBar

```typescript
type navigationBar = navigationBar;
```

### NavigationBarContext

Provided by NavigationBar; read by NavDrawer and the utility links.

```typescript
type NavigationBarContext = React.Context<NavigationBarContextValue | null>;
```

### NavigationBarContextValue

What the header shares with its drawer (§11.6) and utility bar.

```typescript
type NavigationBarContextValue = {
  /** The header's preset; the drawer sheet re-declares it [D97]. */
  preset: BandPreset;
  primary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple';
  secondary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | 'amber'
    | 'blue'
    | 'cyan'
    | 'grass'
    | 'green'
    | 'jade'
    | 'lime'
    | 'mint'
    | 'orange'
    | 'sky'
    | 'teal'
    | 'yellow';
  /** The logo, repeated in the drawer's bar. */
  logo?: React.ReactNode;
  logoHref: string;
  logoLabel?: string;
  /** The current page's URL path. */
  currentPath?: string;
};
```

### NavigationBarDock

How the header docks \[D183]:

- `none` (default): static; it scrolls away with the page \[D174].
- `pinned`: a page without a section bar; the header docks after 50vh of
  scroll and then stays in both directions, until its own place scrolls
  back into view.
- `swap`: a page with a sticky section bar; once the bar's in-flow
  sentinel (`sentinelRef`) has left the viewport top, any upward scroll
  brings the header back in place of the bar and downward scroll restores
  the bar. Hide the section bar while `onDockChange` reports `true`.

```typescript
type NavigationBarDock = 'none' | 'pinned' | 'swap';
```

### NavigationBarProps

Props for NavigationBar.

```typescript
type NavigationBarProps = {
  /**
   * The header preset: a page ground (`paper`, `white` or the page's
   * pastel), or `night` only directly over the night media hero. Omitted,
   * the header takes the page ground it sits in.
   */
  preset?: BandPreset;
  /** Override the preset's primary (a pairing §2 verifies). Never defaulted [D133]. */
  primary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | null;
  /** Override the preset's secondary. Never defaulted. */
  secondary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | 'amber'
    | 'blue'
    | 'cyan'
    | 'grass'
    | 'green'
    | 'jade'
    | 'lime'
    | 'mint'
    | 'orange'
    | 'sky'
    | 'teal'
    | 'yellow'
    | null;
  /** The logo: an ink logo in `currentColor` (an `svg` or `img`), 28 px tall, 36 px from --lg-n-above. */
  logo: React.ReactNode;
  /** The home link's accessible name, e.g. "FairGarden home". */
  logoLabel: string;
  /** The home link. Default "/". */
  logoHref?: string;
  /** An optional fine-line brand drawing (--ds-stroke-0-75) beside the logo; dropped when compact. */
  brandDrawing?: React.ReactNode;
  /** A `NavigationBarUtility` row above the bar, from --ds-nav-inline-n-above; never docks. */
  utility?: React.ReactNode;
  /** The `NavigationMenu`, inline from --ds-nav-inline-n-above. */
  children?: React.ReactNode;
  /** Icon Buttons, e.g. `<Search kind="trigger" … />`, `--size-px-1` apart. */
  search?: React.ReactNode;
  /** The one primary action: a `solid` Button at `size="sm"`. Below 480 px it moves to the drawer footer, so repeat it there. */
  action?: React.ReactNode;
  /** The `NavDrawer` (menu Button and sheet), shown below --ds-nav-inline-n-above. */
  drawer?: React.ReactNode;
  /** The current page's URL path, shared with the menu, drawer and utility links. */
  currentPath?: string;
  /** The skip link's target. Default "#main". */
  skipHref?: string;
  /** The skip link's words. Default "Skip to main content". */
  skipLabel?: string;
  /** `true`: the stacked masthead. Default `false`. */
  masthead?: boolean | null;
  /** `true`: vertical --border-size-1 rules between items. Default `false`. */
  ruled?: boolean | null;
  /** `true`: the compact header, even undocked. The sticky observer sets it while docked. Default `false`. */
  compact?: boolean | null;
  /** How the header docks. Default `none`. */
  dock?: NavigationBarDock;
  /** With `dock="swap"`: a zero-height element at the section bar's in-flow position. */
  sentinelRef?: React.RefObject<Element | null>;
  /** Called when the header docks (`true`) or returns to its place (`false`). */
  onDockChange?: (docked: boolean) => void;
};
```

### NavigationBarUtilityLinkProps

Props for NavigationBarUtilityLink: anchor props.

```typescript
type NavigationBarUtilityLinkProps = React.ComponentProps<'a'>;
```

### NavigationBarUtilityProps

Props for NavigationBarUtility: `nav` props, the start slot and the locale link.

```typescript
type NavigationBarUtilityProps = {
  /** An optional start slot: a phone number or tagline, in --primary12. */
  start?: React.ReactNode;
  /** An optional locale link at the end ("English ▾ Change language"); a control never sits here. */
  locale?: React.ReactNode;
  /** The landmark's name. Default "Utility". */
  label?: string;
  /** The row's `NavigationBarUtilityLink`s: text links only. */
  children?: React.ReactNode;
};
```

## External Types

### BandPreset

```typescript
type BandPreset =
  'night' | 'paper' | 'white' | 'tide' | 'meadow' | 'pollen' | 'apricot' | 'rose' | 'heather';
```

## Export Groups

- `NavigationBar`: `NavigationBar`, `NavigationBarUtility`, `NavigationBarUtilityLink`, `navigationBar`, `NavigationBarContextValue`, `NavigationBarContext`, `NavigationBarDock`, `NavigationBarProps`, `NavigationBarUtilityProps`, `NavigationBarUtilityLinkProps`
- `NavigationBarUtility`: `NavigationBar`, `NavigationBarUtility`, `NavigationBarUtilityLink`, `navigationBar`, `NavigationBarContextValue`, `NavigationBarContext`, `NavigationBarDock`, `NavigationBarProps`, `NavigationBarUtilityProps`, `NavigationBarUtilityLinkProps`
- `NavigationBarUtilityLink`: `NavigationBar`, `NavigationBarUtility`, `NavigationBarUtilityLink`, `navigationBar`, `NavigationBarContextValue`, `NavigationBarContext`, `NavigationBarDock`, `NavigationBarProps`, `NavigationBarUtilityProps`, `NavigationBarUtilityLinkProps`
