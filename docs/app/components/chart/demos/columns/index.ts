import { createDemo } from '@/functions/createDemo';
import { ChartColumns } from './ChartColumns';

export const DemoChartColumns = createDemo(import.meta.url, ChartColumns, {
  name: 'Columns from 1024 px',
  slug: 'columns',
});
