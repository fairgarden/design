# Faq

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate components/faq'

## API Reference

### FAQ

The FAQ module: `FAQHeader` (with `FAQTitle` and an optional `FAQIntro`),
`FAQList` holding `FAQItem`s, and an optional closing `FAQContact`. Place
it on light or deep grounds; on saturated grounds keep to five items with
answers of 40 words or fewer. Every answer prints expanded.

**FAQ Props:**

| Prop      | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                                                                                                                                                                                        |
| :-------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| barred    | `boolean \| null`                                                                                                                                                                                                                                                                                                                                  | -       | Shows a `--ds-stroke-3` `--primary12` bar at the start of each open&#xA;item's row: a bolder line, never a fill. Default `false`.                                                                                                                                                  |
| kind      | `'ruled' \| 'keyed' \| 'split' \| null`                                                                                                                                                                                                                                                                                                            | -       | `ruled` (default): bare rows between `--role-rule` rules. `keyed`: a&#xA;leading block icon per row (`FAQItem icon`), answers indented to the&#xA;question column. `split`: from 1024 px of viewport, header and intro in&#xA;the left third and the list in the right two thirds. |
| primary   | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale: rules, questions, glyph and answers. Never&#xA;defaulted; omitted, it inherits the scope \[D133].                                                                                                                                                             |
| secondary | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale: only the link underlines inside answers and on&#xA;the contact link (`--role-accent`). Never defaulted.                                                                                                                                                     |

### FAQContact

The optional closing contact line, e.g. "Still stuck? Write to us." with a Link.

### FAQHeader

The module header: the chapter-stack title, then the optional intro (§3.5 Recipe A).

### FAQIntro

An optional lede under the title, within `--ds-measure-reading`.

### FAQItem

One question and its answer. The question is an `h3` wrapping the trigger;
the disclosure glyph trails it, top-aligned to its first line. The answer
is `type-body`, within `--ds-measure-reading`. FAQ items are never
disabled.

**FAQItem Props:**

| Prop       | Type                                                                                                                                                                                                                                                                                         | Default | Description                                                                                                                              |
| :--------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :--------------------------------------------------------------------------------------------------------------------------------------- |
| icon       | `'menu' \| 'search' \| 'circle' \| 'arrow_forward' \| 'arrow_upward' \| 'expand_more' \| 'close' \| 'remove' \| 'add' \| 'check' \| 'chevron_right' \| 'chevron_left' \| 'more_horiz' \| 'play_arrow' \| 'pause' \| 'download' \| 'zoom_in' \| 'zoom_out' \| 'recenter' \| 'help' \| 'mail'` | -       | The leading block icon of the `keyed` kind (§6.10 subject symbol,&#xA;Material Symbols Rounded, block tier). Ignored by the other kinds. |
| panelProps | `Omit<AccordionPanelProps, 'children'>`                                                                                                                                                                                                                                                      | -       | Props for the answer panel, such as `keepMounted`.                                                                                       |
| question   | `React.ReactNode`                                                                                                                                                                                                                                                                            | -       | The question: `type-itemhead` in `--primary12`, inside an `h3`.                                                                          |

### FAQList

The item list, a Base UI Accordion Root. Several items may be open at once
(`multiple`, default `true`); closed answers stay in the DOM
(`hiddenUntilFound`, default `true`) for find-in-page and print. Never
open items by hover.

### FAQTitle

The section head: `type-h2` in `--role-heading`. Renders `<h2>`.

## Additional Types

### faq

```typescript
type faq = faq;
```

### FAQContactProps

Props for FAQContact: paragraph props and `render`.

```typescript
type FAQContactProps = useRender.ComponentProps<'p'>;
```

### FAQHeaderProps

Props for FAQHeader: `header` props and `render`.

```typescript
type FAQHeaderProps = useRender.ComponentProps<'header'>;
```

### FAQIntroProps

Props for FAQIntro: paragraph props and `render`.

```typescript
type FAQIntroProps = useRender.ComponentProps<'p'>;
```

### FAQItemProps

Props for FAQItem: Base UI Accordion Item props (without `disabled`) plus the question.

```typescript
type FAQItemProps = {
  /** The question: `type-itemhead` in `--primary12`, inside an `h3`. */
  question: React.ReactNode;
  /**
   * The leading block icon of the `keyed` kind (§6.10 subject symbol,
   * Material Symbols Rounded, block tier). Ignored by the other kinds.
   */
  icon?:
    | 'menu'
    | 'search'
    | 'circle'
    | 'arrow_forward'
    | 'arrow_upward'
    | 'expand_more'
    | 'close'
    | 'remove'
    | 'add'
    | 'check'
    | 'chevron_right'
    | 'chevron_left'
    | 'more_horiz'
    | 'play_arrow'
    | 'pause'
    | 'download'
    | 'zoom_in'
    | 'zoom_out'
    | 'recenter'
    | 'help'
    | 'mail';
  /** Props for the answer panel, such as `keepMounted`. */
  panelProps?: Omit<AccordionPanelProps, 'children'>;
};
```

### FAQListProps

Props for FAQList: Base UI Accordion Root props.

```typescript
type FAQListProps<Value = unknown> = AccordionRoot.Props<Value>;
```

### FAQProps

Props for FAQ: `section` props, `render`, and the kind, bar and color axes.

```typescript
type FAQProps = React.ClassAttributes<HTMLElement> &
  React.HTMLAttributes<HTMLElement> &
  __type & {
    kind?: 'ruled' | 'keyed' | 'split' | null;
    barred?: boolean | null;
    primary?:
      | 'ruby'
      | 'olive'
      | 'sage'
      | 'slate'
      | 'sand'
      | 'gray'
      | 'mauve'
      | 'brown'
      | 'bronze'
      | 'gold'
      | 'red'
      | 'crimson'
      | 'tomato'
      | 'pink'
      | 'plum'
      | 'indigo'
      | 'iris'
      | 'violet'
      | 'purple'
      | null;
    secondary?:
      | 'ruby'
      | 'olive'
      | 'sage'
      | 'slate'
      | 'sand'
      | 'gray'
      | 'mauve'
      | 'brown'
      | 'bronze'
      | 'gold'
      | 'red'
      | 'crimson'
      | 'tomato'
      | 'pink'
      | 'plum'
      | 'indigo'
      | 'iris'
      | 'violet'
      | 'purple'
      | 'amber'
      | 'blue'
      | 'cyan'
      | 'grass'
      | 'green'
      | 'jade'
      | 'lime'
      | 'mint'
      | 'orange'
      | 'sky'
      | 'teal'
      | 'yellow'
      | null;
  };
```

### FAQTitleProps

Props for FAQTitle: heading props and `render` (default `<h2>`).

```typescript
type FAQTitleProps = useRender.ComponentProps<'h2'>;
```

## Export Groups

- `FAQ`: `FAQ`, `FAQHeader`, `FAQTitle`, `FAQIntro`, `FAQContact`, `FAQList`, `FAQItem`, `faq`, `FAQProps`, `FAQHeaderProps`, `FAQTitleProps`, `FAQIntroProps`, `FAQContactProps`, `FAQListProps`, `FAQItemProps`
- `FAQHeader`: `FAQ`, `FAQHeader`, `FAQTitle`, `FAQIntro`, `FAQContact`, `FAQList`, `FAQItem`, `faq`, `FAQProps`, `FAQHeaderProps`, `FAQTitleProps`, `FAQIntroProps`, `FAQContactProps`, `FAQListProps`, `FAQItemProps`
- `FAQTitle`: `FAQ`, `FAQHeader`, `FAQTitle`, `FAQIntro`, `FAQContact`, `FAQList`, `FAQItem`, `faq`, `FAQProps`, `FAQHeaderProps`, `FAQTitleProps`, `FAQIntroProps`, `FAQContactProps`, `FAQListProps`, `FAQItemProps`
- `FAQIntro`: `FAQ`, `FAQHeader`, `FAQTitle`, `FAQIntro`, `FAQContact`, `FAQList`, `FAQItem`, `faq`, `FAQProps`, `FAQHeaderProps`, `FAQTitleProps`, `FAQIntroProps`, `FAQContactProps`, `FAQListProps`, `FAQItemProps`
- `FAQList`: `FAQ`, `FAQHeader`, `FAQTitle`, `FAQIntro`, `FAQContact`, `FAQList`, `FAQItem`, `faq`, `FAQProps`, `FAQHeaderProps`, `FAQTitleProps`, `FAQIntroProps`, `FAQContactProps`, `FAQListProps`, `FAQItemProps`
- `FAQItem`: `FAQ`, `FAQHeader`, `FAQTitle`, `FAQIntro`, `FAQContact`, `FAQList`, `FAQItem`, `faq`, `FAQProps`, `FAQHeaderProps`, `FAQTitleProps`, `FAQIntroProps`, `FAQContactProps`, `FAQListProps`, `FAQItemProps`
- `FAQContact`: `FAQ`, `FAQHeader`, `FAQTitle`, `FAQIntro`, `FAQContact`, `FAQList`, `FAQItem`, `faq`, `FAQProps`, `FAQHeaderProps`, `FAQTitleProps`, `FAQIntroProps`, `FAQContactProps`, `FAQListProps`, `FAQItemProps`
