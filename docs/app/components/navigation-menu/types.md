# Navigation Menu

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate components/navigation-menu'

## API Reference

### isCurrentPath

`true` when `href` is the current page: its path equals `currentPath`.

**Parameters:**

| Parameter   | Type                          | Default | Description |
| :---------- | :---------------------------- | :------ | :---------- |
| href        | `string \| null \| undefined` | -       | -           |
| currentPath | `string \| null \| undefined` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = boolean;
```

### isInSection

`true` when the page lies in the section at `sectionHref`: the section's
path is a whole-segment prefix of `currentPath` (the section page itself
included). The home path `/` matches only itself.

**Parameters:**

| Parameter   | Type                          | Default | Description |
| :---------- | :---------------------------- | :------ | :---------- |
| sectionHref | `string \| null \| undefined` | -       | -           |
| currentPath | `string \| null \| undefined` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = boolean;
```

### NavigationMenu

The desktop site navigation \[D182]: a `nav` labelled "Main" holding the
bar's items. Triggers are buttons that open their panel on click, Enter,
Space or ArrowDown, and on hover intent under a mouse
(--ds-delay-nav-open, closing --ds-delay-nav-close after the pointer
leaves); focus alone never opens one. Opening from the keyboard moves
focus to the panel's first link; Escape closes and returns focus to the
trigger. Panels attach at 0 offset under the bar's bottom rule, switch
instantly and scroll in a Scroll Area when taller than the viewport.

**NavigationMenu Props:**

| Prop        | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                                                                                                                                                                                |
| :---------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| label       | `string`                                                                                                                                                                                                                                                                                                                                           | -       | The landmark's name. Default "Main".                                                                                                                                                                                                                                       |
| currentPath | `string`                                                                                                                                                                                                                                                                                                                                           | -       | The current page's URL path. A link whose path equals it is current&#xA;(`aria-current="page"`); a bar item whose section is a path prefix of it&#xA;is the parent of current (`aria-current="true"`). Inside a Navigation&#xA;Bar it defaults to the bar's `currentPath`. |
| primary     | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale for the bar's parts: labels, bars and focus ring.&#xA;Never defaulted; omitted, it inherits the scope \[D133]. The panels keep&#xA;the `white` preset's scales.                                                                                        |
| secondary   | `'ruby' \| 'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale for the bar's parts: the chevrons and the hover&#xA;underline (`--role-glyph`, `--role-accent`). Never defaulted.                                                                                                                                    |
| children    | `React.ReactNode`                                                                                                                                                                                                                                                                                                                                  | -       | `NavigationMenuItem`s, in bar order.                                                                                                                                                                                                                                       |

### NavigationMenuCategory

One category of an `index` panel: a row in the category list (a trigger
of the nested vertical menu) and its links in the pane. The selected
category takes a `--ds-stroke-3` start-edge bar and `--font-weight-7`.

**NavigationMenuCategory Props:**

| Prop      | Type              | Default | Description                                                   |
| :-------- | :---------------- | :------ | :------------------------------------------------------------ |
| label\*   | `React.ReactNode` | -       | The category row's label.                                     |
| value     | `string`          | -       | Identifies the category; omitted, the panel assigns one.      |
| children  | `React.ReactNode` | -       | The category's `NavigationMenuLink`s, shown in the link pane. |
| className | `string`          | -       | Class names for the category row, added after its own.        |

### NavigationMenuContent

A panel. It renders in the menu's portaled `white` overlay scope,
attached under the bar's bottom rule: `overview` full-bleed with its
content on the 12-column grid of `--ds-container-content`; `index` and
`dropdown` at the trigger's start edge, end-aligned to the content
container, with square top and `--ds-radius-8` bottom corners.

**NavigationMenuContent Props:**

| Prop        | Type                                  | Default | Description                                                             |
| :---------- | :------------------------------------ | :------ | :---------------------------------------------------------------------- |
| featured    | `React.ReactNode`                     | -       | -                                                                       |
| featuredBar | `React.ReactNode`                     | -       | -                                                                       |
| kind        | `'dropdown' \| 'overview' \| 'index'` | -       | `dropdown` (default): a simple list panel of 3–8 `NavigationMenuLink`s. |
| promo       | `React.ReactNode`                     | -       | -                                                                       |
| search      | `React.ReactNode`                     | -       | -                                                                       |
| children    | `React.ReactNode`                     | -       | -                                                                       |

### NavigationMenuFeatured

The overview link (`featured`): the trigger's own section landing page, a
`type-itemhead` serif link in `--role-heading` with a trailing › in
`--role-accent`, then its description. Put it in `featured`.

**NavigationMenuFeatured Props:**

| Prop        | Type              | Default | Description                                                  |
| :---------- | :---------------- | :------ | :----------------------------------------------------------- |
| description | `React.ReactNode` | -       | One line of `type-body-ui` in `--role-muted` under the link. |

### NavigationMenuFeaturedBar

The featured bar of an `index` panel: one `type-itemhead` link with a ›
in `--role-glyph`, across both panes above a `--role-hairline` rule. Put
it in `featuredBar`.

### NavigationMenuGroup

A caps link group in an `overview` panel: a heading (a link with › when
`href` is given) over its links at a `--ds-size-hit` pitch.

**NavigationMenuGroup Props:**

| Prop     | Type              | Default | Description                                                              |
| :------- | :---------------- | :------ | :----------------------------------------------------------------------- |
| heading  | `React.ReactNode` | -       | The group heading, set in `type-label` caps.                             |
| href     | `string`          | -       | Makes the heading a link with a trailing ›, to the group's landing page. |
| children | `React.ReactNode` | -       | The group's `NavigationMenuLink`s.                                       |

### NavigationMenuItem

One bar item: a `NavigationMenuTrigger` with its `NavigationMenuContent`,
or a plain `NavigationMenuLink`. `value` identifies it when the menu is
controlled; omitted, one is generated.

### NavigationMenuLink

A link. In the bar it is a caps item with no rest underline; in a panel
it is a `type-body-ui` row at a `--ds-size-hit` pitch in Link's list-link
build (`kind="nav"` with `list`), whose hover is `--role-link-hover` only
(§9.3) \[D181]. `active` defaults to "its path is the current page"
(`aria-current="page"`); a bar link whose path is a prefix of the page is
the parent of current. Panel links close the panel on click.

### NavigationMenuPromo

The optional promo card of an `overview` panel (≤ 1 per panel): a §12.2
card with a 16:9 image, a stretched title link (one Tab stop) and a
blurb. Its hover is the title link's `--role-link-hover`; no shadow.

**NavigationMenuPromo Props:**

| Prop      | Type              | Default | Description                                                         |
| :-------- | :---------------- | :------ | :------------------------------------------------------------------ |
| href\*    | `string`          | -       | The destination: the card's one Tab stop, its stretched title link. |
| image     | `React.ReactNode` | -       | A 16:9 image (an `img` with its `alt`).                             |
| title\*   | `React.ReactNode` | -       | The title, `type-itemhead`.                                         |
| children  | `React.ReactNode` | -       | A two-line `type-body-ui` blurb.                                    |
| className | `string`          | -       | Class names for the card, added after its own.                      |

### NavigationMenuSections

The bar's items built from the shared navigation data \[D187], the object
that also feeds `NavDrawer`'s `sections` and `Footer`'s `sitemap`. Each
section is a trigger (its `href` marks the parent of current) and the
panel `sitemapPanel` picks: `overview` (the overview link and
description, a caps group per link over its own `links`, the promo),
`index` (a category per link and the featured bar) or `dropdown` (the
landing page, then the links). A section without links is a plain bar
link. Put it inside `NavigationMenu`, alone or beside hand-built items.

**NavigationMenuSections Props:**

| Prop       | Type               | Default | Description                                                                                                                                       |
| :--------- | :----------------- | :------ | :------------------------------------------------------------------------------------------------------------------------------------------------ |
| sections\* | `SitemapSection[]` | -       | The shared navigation data (utils/navigation): the top-level sections,&#xA;in bar order. The same object feeds the drawer and the footer sitemap. |

### NavigationMenuTrigger

A bar trigger: a button with a caps label (`type-label`, \[D165]) and the
`expand_more` chevron in `--role-glyph`, which flips while open. It opens
its panel on click, Enter, Space, ArrowDown or mouse hover intent, never
on focus alone; the section's landing page is the panel's first link.

**NavigationMenuTrigger Props:**

| Prop    | Type     | Default | Description                                                                                                                                                                                   |
| :------ | :------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| section | `string` | -       | The path of the section this trigger opens (`/what-we-do`). While the&#xA;page lies in it, the trigger is the parent of current&#xA;(`aria-current="true"`, the --border-size-2 bar) \[D182]. |

### toPath

The path of a site-relative (or absolute http) href, without query, hash
or trailing slash; `null` for fragments and other protocols.

**Parameters:**

| Parameter | Type                          | Default | Description |
| :-------- | :---------------------------- | :------ | :---------- |
| href      | `string \| null \| undefined` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = string | null;
```

## Additional Types

### NAV_DELAY_MS

```typescript
type NAV_DELAY_MS = { open: 150; close: 300 };
```

### navigationMenu

```typescript
type navigationMenu = navigationMenu;
```

### NavigationMenuCategoryProps

Props for NavigationMenuCategory.

```typescript
type NavigationMenuCategoryProps = {
  /** The category row's label. */
  label: React.ReactNode;
  /** Identifies the category; omitted, the panel assigns one. */
  value?: string;
  /** The category's `NavigationMenuLink`s, shown in the link pane. */
  children?: React.ReactNode;
  /** Class names for the category row, added after its own. */
  className?: string;
};
```

### navigationMenuContent

Panel classes: `kind` → dropdown (default) | overview | index \[D182].

```typescript
type navigationMenuContent = navigationMenuContent;
```

### NavigationMenuContentKind

The panel kinds of `navigationMenuContent` \[D182].

```typescript
type NavigationMenuContentKind = NonNullable<'dropdown' | 'overview' | 'index' | null | undefined>;
```

### NavigationMenuContentProps

Props for NavigationMenuContent: Base UI Content props and the panel
`kind`, with each kind's slots. A discriminated union on `kind`.

```typescript
type NavigationMenuContentProps =
  | {
      children?: React.ReactNode;
      kind?: 'dropdown';
      featured?: undefined;
      search?: undefined;
      promo?: undefined;
      featuredBar?: undefined;
    }
  | {
      children?: React.ReactNode;
      kind: 'overview';
      featured?: React.ReactNode;
      search?: React.ReactNode;
      promo?: React.ReactNode;
      featuredBar?: undefined;
    }
  | {
      children?: React.ReactNode;
      kind: 'index';
      featuredBar?: React.ReactNode;
      featured?: undefined;
      search?: undefined;
      promo?: undefined;
    };
```

### NavigationMenuFeaturedBarProps

Props for NavigationMenuFeaturedBar: Base UI Link props.

```typescript
type NavigationMenuFeaturedBarProps = NavigationMenuLink.NavigationMenuLinkProps;
```

### NavigationMenuFeaturedProps

Props for NavigationMenuFeatured: Base UI Link props plus the one-line description.

```typescript
type NavigationMenuFeaturedProps = NavigationMenuLinkProps & { description?: React.ReactNode };
```

### NavigationMenuFrame

What a host frame (the Navigation Bar) tells its menu: the bar whose
bottom rule the panels attach under, the content container an `index` or
`dropdown` panel end-aligns inside, and the current page's path.

```typescript
type NavigationMenuFrame = {
  /** The bar element; panels attach at 0 offset under its bottom rule. Default: the menu's `nav`. */
  anchorRef?: React.RefObject<HTMLElement | null>;
  /** The content container (--ds-container-content). Default: the anchor. */
  boundaryRef?: React.RefObject<HTMLElement | null>;
  /** The current page's URL path, for current and parent-of-current. */
  currentPath?: string;
};
```

### NavigationMenuFrameContext

Provided by the Navigation Bar (§11.5); a menu outside a bar uses its own `nav`.

```typescript
type NavigationMenuFrameContext = React.Context<NavigationMenuFrame>;
```

### NavigationMenuGroupProps

Props for NavigationMenuGroup: `div` props, the heading and its optional link.

```typescript
type NavigationMenuGroupProps = {
  /** The group heading, set in `type-label` caps. */
  heading: React.ReactNode;
  /** Makes the heading a link with a trailing ›, to the group's landing page. */
  href?: string;
  /** The group's `NavigationMenuLink`s. */
  children?: React.ReactNode;
};
```

### NavigationMenuItemProps

Props for NavigationMenuItem: Base UI Item props.

```typescript
type NavigationMenuItemProps = NavigationMenuItem.NavigationMenuItemProps;
```

### NavigationMenuLinkProps

Props for NavigationMenuLink: Base UI Link props.

```typescript
type NavigationMenuLinkProps = NavigationMenuLink.NavigationMenuLinkProps;
```

### NavigationMenuPromoProps

Props for NavigationMenuPromo.

```typescript
type NavigationMenuPromoProps = {
  /** The destination: the card's one Tab stop, its stretched title link. */
  href: string;
  /** The title, `type-itemhead`. */
  title: React.ReactNode;
  /** A 16:9 image (an `img` with its `alt`). */
  image?: React.ReactNode;
  /** A two-line `type-body-ui` blurb. */
  children?: React.ReactNode;
  /** Class names for the card, added after its own. */
  className?: string;
};
```

### NavigationMenuProps

Props for NavigationMenu: Base UI Root props (without the delays), the color axes and the page path.

```typescript
type NavigationMenuProps = {
  /**
   * Primary Radix scale for the bar's parts: labels, bars and focus ring.
   * Never defaulted; omitted, it inherits the scope [D133]. The panels keep
   * the `white` preset's scales.
   */
  primary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | null;
  /**
   * Secondary Radix scale for the bar's parts: the chevrons and the hover
   * underline (`--role-glyph`, `--role-accent`). Never defaulted.
   */
  secondary?:
    | 'ruby'
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | 'amber'
    | 'blue'
    | 'cyan'
    | 'grass'
    | 'green'
    | 'jade'
    | 'lime'
    | 'mint'
    | 'orange'
    | 'sky'
    | 'teal'
    | 'yellow'
    | null;
  /**
   * The current page's URL path. A link whose path equals it is current
   * (`aria-current="page"`); a bar item whose section is a path prefix of it
   * is the parent of current (`aria-current="true"`). Inside a Navigation
   * Bar it defaults to the bar's `currentPath`.
   */
  currentPath?: string;
  /** The landmark's name. Default "Main". */
  label?: string;
  /** `NavigationMenuItem`s, in bar order. */
  children?: React.ReactNode;
};
```

### NavigationMenuSectionsProps

Props for NavigationMenuSections.

```typescript
type NavigationMenuSectionsProps = {
  /**
   * The shared navigation data (utils/navigation): the top-level sections,
   * in bar order. The same object feeds the drawer and the footer sitemap.
   */
  sections: SitemapSection[];
};
```

### NavigationMenuTriggerProps

Props for NavigationMenuTrigger: Base UI Trigger props plus the section path.

```typescript
type NavigationMenuTriggerProps = NavigationMenuTriggerProps & { section?: string };
```

## External Types

### NonNullable

```typescript
type NonNullable = 'dropdown' | 'overview' | 'index';
```

## Export Groups

- `NavigationMenu`: `NavigationMenu`, `NavigationMenuItem`, `NavigationMenuTrigger`, `NavigationMenuLink`, `NavigationMenuContent`, `NavigationMenuFeatured`, `NavigationMenuGroup`, `NavigationMenuPromo`, `NavigationMenuCategory`, `NavigationMenuFeaturedBar`, `NavigationMenuSections`, `NAV_DELAY_MS`, `navigationMenu`, `navigationMenuContent`, `NavigationMenuContentKind`, `toPath`, `isCurrentPath`, `isInSection`, `NavigationMenuFrame`, `NavigationMenuFrameContext`, `NavigationMenuProps`, `NavigationMenuItemProps`, `NavigationMenuTriggerProps`, `NavigationMenuLinkProps`, `NavigationMenuContentProps`, `NavigationMenuFeaturedProps`, `NavigationMenuGroupProps`, `NavigationMenuPromoProps`, `NavigationMenuCategoryProps`, `NavigationMenuFeaturedBarProps`, `NavigationMenuSectionsProps`
- `NavigationMenuItem`: `NavigationMenu`, `NavigationMenuItem`, `NavigationMenuTrigger`, `NavigationMenuLink`, `NavigationMenuContent`, `NavigationMenuFeatured`, `NavigationMenuGroup`, `NavigationMenuPromo`, `NavigationMenuCategory`, `NavigationMenuFeaturedBar`, `NavigationMenuSections`, `NAV_DELAY_MS`, `navigationMenu`, `navigationMenuContent`, `NavigationMenuContentKind`, `toPath`, `isCurrentPath`, `isInSection`, `NavigationMenuFrame`, `NavigationMenuFrameContext`, `NavigationMenuProps`, `NavigationMenuItemProps`, `NavigationMenuTriggerProps`, `NavigationMenuLinkProps`, `NavigationMenuContentProps`, `NavigationMenuFeaturedProps`, `NavigationMenuGroupProps`, `NavigationMenuPromoProps`, `NavigationMenuCategoryProps`, `NavigationMenuFeaturedBarProps`, `NavigationMenuSectionsProps`
- `NavigationMenuTrigger`: `NavigationMenu`, `NavigationMenuItem`, `NavigationMenuTrigger`, `NavigationMenuLink`, `NavigationMenuContent`, `NavigationMenuFeatured`, `NavigationMenuGroup`, `NavigationMenuPromo`, `NavigationMenuCategory`, `NavigationMenuFeaturedBar`, `NavigationMenuSections`, `NAV_DELAY_MS`, `navigationMenu`, `navigationMenuContent`, `NavigationMenuContentKind`, `toPath`, `isCurrentPath`, `isInSection`, `NavigationMenuFrame`, `NavigationMenuFrameContext`, `NavigationMenuProps`, `NavigationMenuItemProps`, `NavigationMenuTriggerProps`, `NavigationMenuLinkProps`, `NavigationMenuContentProps`, `NavigationMenuFeaturedProps`, `NavigationMenuGroupProps`, `NavigationMenuPromoProps`, `NavigationMenuCategoryProps`, `NavigationMenuFeaturedBarProps`, `NavigationMenuSectionsProps`
- `NavigationMenuLink`: `NavigationMenu`, `NavigationMenuItem`, `NavigationMenuTrigger`, `NavigationMenuLink`, `NavigationMenuContent`, `NavigationMenuFeatured`, `NavigationMenuGroup`, `NavigationMenuPromo`, `NavigationMenuCategory`, `NavigationMenuFeaturedBar`, `NavigationMenuSections`, `NAV_DELAY_MS`, `navigationMenu`, `navigationMenuContent`, `NavigationMenuContentKind`, `toPath`, `isCurrentPath`, `isInSection`, `NavigationMenuFrame`, `NavigationMenuFrameContext`, `NavigationMenuProps`, `NavigationMenuItemProps`, `NavigationMenuTriggerProps`, `NavigationMenuLinkProps`, `NavigationMenuContentProps`, `NavigationMenuFeaturedProps`, `NavigationMenuGroupProps`, `NavigationMenuPromoProps`, `NavigationMenuCategoryProps`, `NavigationMenuFeaturedBarProps`, `NavigationMenuSectionsProps`
- `NavigationMenuContent`: `NavigationMenu`, `NavigationMenuItem`, `NavigationMenuTrigger`, `NavigationMenuLink`, `NavigationMenuContent`, `NavigationMenuFeatured`, `NavigationMenuGroup`, `NavigationMenuPromo`, `NavigationMenuCategory`, `NavigationMenuFeaturedBar`, `NavigationMenuSections`, `NAV_DELAY_MS`, `navigationMenu`, `navigationMenuContent`, `NavigationMenuContentKind`, `toPath`, `isCurrentPath`, `isInSection`, `NavigationMenuFrame`, `NavigationMenuFrameContext`, `NavigationMenuProps`, `NavigationMenuItemProps`, `NavigationMenuTriggerProps`, `NavigationMenuLinkProps`, `NavigationMenuContentProps`, `NavigationMenuFeaturedProps`, `NavigationMenuGroupProps`, `NavigationMenuPromoProps`, `NavigationMenuCategoryProps`, `NavigationMenuFeaturedBarProps`, `NavigationMenuSectionsProps`
- `NavigationMenuFeatured`: `NavigationMenu`, `NavigationMenuItem`, `NavigationMenuTrigger`, `NavigationMenuLink`, `NavigationMenuContent`, `NavigationMenuFeatured`, `NavigationMenuGroup`, `NavigationMenuPromo`, `NavigationMenuCategory`, `NavigationMenuFeaturedBar`, `NavigationMenuSections`, `NAV_DELAY_MS`, `navigationMenu`, `navigationMenuContent`, `NavigationMenuContentKind`, `toPath`, `isCurrentPath`, `isInSection`, `NavigationMenuFrame`, `NavigationMenuFrameContext`, `NavigationMenuProps`, `NavigationMenuItemProps`, `NavigationMenuTriggerProps`, `NavigationMenuLinkProps`, `NavigationMenuContentProps`, `NavigationMenuFeaturedProps`, `NavigationMenuGroupProps`, `NavigationMenuPromoProps`, `NavigationMenuCategoryProps`, `NavigationMenuFeaturedBarProps`, `NavigationMenuSectionsProps`
- `NavigationMenuGroup`: `NavigationMenu`, `NavigationMenuItem`, `NavigationMenuTrigger`, `NavigationMenuLink`, `NavigationMenuContent`, `NavigationMenuFeatured`, `NavigationMenuGroup`, `NavigationMenuPromo`, `NavigationMenuCategory`, `NavigationMenuFeaturedBar`, `NavigationMenuSections`, `NAV_DELAY_MS`, `navigationMenu`, `navigationMenuContent`, `NavigationMenuContentKind`, `toPath`, `isCurrentPath`, `isInSection`, `NavigationMenuFrame`, `NavigationMenuFrameContext`, `NavigationMenuProps`, `NavigationMenuItemProps`, `NavigationMenuTriggerProps`, `NavigationMenuLinkProps`, `NavigationMenuContentProps`, `NavigationMenuFeaturedProps`, `NavigationMenuGroupProps`, `NavigationMenuPromoProps`, `NavigationMenuCategoryProps`, `NavigationMenuFeaturedBarProps`, `NavigationMenuSectionsProps`
- `NavigationMenuPromo`: `NavigationMenu`, `NavigationMenuItem`, `NavigationMenuTrigger`, `NavigationMenuLink`, `NavigationMenuContent`, `NavigationMenuFeatured`, `NavigationMenuGroup`, `NavigationMenuPromo`, `NavigationMenuCategory`, `NavigationMenuFeaturedBar`, `NavigationMenuSections`, `NAV_DELAY_MS`, `navigationMenu`, `navigationMenuContent`, `NavigationMenuContentKind`, `toPath`, `isCurrentPath`, `isInSection`, `NavigationMenuFrame`, `NavigationMenuFrameContext`, `NavigationMenuProps`, `NavigationMenuItemProps`, `NavigationMenuTriggerProps`, `NavigationMenuLinkProps`, `NavigationMenuContentProps`, `NavigationMenuFeaturedProps`, `NavigationMenuGroupProps`, `NavigationMenuPromoProps`, `NavigationMenuCategoryProps`, `NavigationMenuFeaturedBarProps`, `NavigationMenuSectionsProps`
- `NavigationMenuCategory`: `NavigationMenu`, `NavigationMenuItem`, `NavigationMenuTrigger`, `NavigationMenuLink`, `NavigationMenuContent`, `NavigationMenuFeatured`, `NavigationMenuGroup`, `NavigationMenuPromo`, `NavigationMenuCategory`, `NavigationMenuFeaturedBar`, `NavigationMenuSections`, `NAV_DELAY_MS`, `navigationMenu`, `navigationMenuContent`, `NavigationMenuContentKind`, `toPath`, `isCurrentPath`, `isInSection`, `NavigationMenuFrame`, `NavigationMenuFrameContext`, `NavigationMenuProps`, `NavigationMenuItemProps`, `NavigationMenuTriggerProps`, `NavigationMenuLinkProps`, `NavigationMenuContentProps`, `NavigationMenuFeaturedProps`, `NavigationMenuGroupProps`, `NavigationMenuPromoProps`, `NavigationMenuCategoryProps`, `NavigationMenuFeaturedBarProps`, `NavigationMenuSectionsProps`
- `NavigationMenuFeaturedBar`: `NavigationMenu`, `NavigationMenuItem`, `NavigationMenuTrigger`, `NavigationMenuLink`, `NavigationMenuContent`, `NavigationMenuFeatured`, `NavigationMenuGroup`, `NavigationMenuPromo`, `NavigationMenuCategory`, `NavigationMenuFeaturedBar`, `NavigationMenuSections`, `NAV_DELAY_MS`, `navigationMenu`, `navigationMenuContent`, `NavigationMenuContentKind`, `toPath`, `isCurrentPath`, `isInSection`, `NavigationMenuFrame`, `NavigationMenuFrameContext`, `NavigationMenuProps`, `NavigationMenuItemProps`, `NavigationMenuTriggerProps`, `NavigationMenuLinkProps`, `NavigationMenuContentProps`, `NavigationMenuFeaturedProps`, `NavigationMenuGroupProps`, `NavigationMenuPromoProps`, `NavigationMenuCategoryProps`, `NavigationMenuFeaturedBarProps`, `NavigationMenuSectionsProps`
