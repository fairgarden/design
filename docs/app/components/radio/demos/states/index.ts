import { createDemo } from '@/functions/createDemo';
import { RadioStates } from './RadioStates';

export const DemoRadioStates = createDemo(import.meta.url, RadioStates, {
  name: 'States and quiz feedback',
  slug: 'states',
});
