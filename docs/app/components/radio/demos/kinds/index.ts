import { createDemo } from '@/functions/createDemo';
import { RadioKinds } from './RadioKinds';

export const DemoRadioKinds = createDemo(import.meta.url, RadioKinds, {
  name: 'Option pills and swatches',
  slug: 'kinds',
});
