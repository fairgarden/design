import { createDemo } from '@/functions/createDemo';
import { RadioGrounds } from './RadioGrounds';

export const DemoRadioGrounds = createDemo(import.meta.url, RadioGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
