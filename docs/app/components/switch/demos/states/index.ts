import { createDemo } from '@/functions/createDemo';
import { SwitchStates } from './SwitchStates';

export const DemoSwitchStates = createDemo(import.meta.url, SwitchStates, {
  name: 'States',
  slug: 'states',
});
