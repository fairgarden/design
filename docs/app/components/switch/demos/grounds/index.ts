import { createDemo } from '@/functions/createDemo';
import { SwitchGrounds } from './SwitchGrounds';

export const DemoSwitchGrounds = createDemo(import.meta.url, SwitchGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
