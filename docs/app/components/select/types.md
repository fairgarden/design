# Select

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate components/select'

## API Reference

### Select

A Base UI Select inside a `Field` (label it with
`<FieldLabel nativeLabel={false}>`). The popup opens below the trigger,
never on hover, and is never a sheet; use Combobox for long or
searchable lists and Radio for 2–4 visible options.

**Select Props:**

| Prop        | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                                       |
| :---------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :-------------------------------------------------------------------------------------------------------------------------------- |
| aria-label  | `string`                                                                                                                                                                                                                                                                                                                                           | -       | Names the trigger when no visible label exists. Prefer a `FieldLabel`.                                                            |
| items       | `SelectItems<Value>`                                                                                                                                                                                                                                                                                                                               | -       | The options (or titled groups). Rendered as items unless `children`&#xA;are given; always used to show the chosen option's label. |
| placeholder | `string`                                                                                                                                                                                                                                                                                                                                           | -       | Shown in --role-muted while nothing is chosen; end it with "…".                                                                   |
| plate       | `boolean \| false`                                                                                                                                                                                                                                                                                                                                 | -       | The trigger's face becomes a nested `white` scope; patterned grounds only (§10.1).                                                |
| primary     | `'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'ruby' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale: trigger edge, value, chevron and focus ring. Never&#xA;defaulted; omitted, it inherits the scope \[D133].    |
| secondary   | `'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'ruby' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale: the underline variant's underline; the danger&#xA;scale while invalid.                                     |
| variant     | `'outline' \| 'underline'`                                                                                                                                                                                                                                                                                                                         | -       | `outline` (default) is the boxed field; `underline` is the trigger&#xA;over a --role-accent underline that hugs its content.      |
| children    | `React.ReactNode`                                                                                                                                                                                                                                                                                                                                  | -       | Custom list content (`SelectItem`, `SelectGroup`, `SelectSeparator`) instead of `items`.                                          |
| className   | `string`                                                                                                                                                                                                                                                                                                                                           | -       | Class names for the trigger (the root part), added after the module's own.                                                        |

### SelectGroup

A titled group of items.

**SelectGroup Props:**

| Prop      | Type              | Default | Description                                             |
| :-------- | :---------------- | :------ | :------------------------------------------------------ |
| label     | `React.ReactNode` | -       | The group label, in `type-label` caps and --role-muted. |
| className | `string`          | -       | -                                                       |

### SelectItem

One option: a `--ds-size-hit` row. Highlighted rows take the `--primary4`
soft fill and the `--ds-stroke-3` start bar; the chosen row a leading ✓
and `--font-weight-6` \[D145].

**SelectItem Props:**

| Prop      | Type     | Default | Description                                                      |
| :-------- | :------- | :------ | :--------------------------------------------------------------- |
| swatch    | `string` | -       | The product's color (content) as a ringed disc before the label. |
| className | `string` | -       | Extra class names, added after the module's own.                 |

### SelectSeparator

The one popup group separator: `--border-size-1` in `--role-rule`.

**SelectSeparator Props:**

| Prop      | Type     | Default | Description |
| :-------- | :------- | :------ | :---------- |
| className | `string` | -       | -           |

## Additional Types

### select

```typescript
type select = select;
```

### SelectGroupProps

Props for SelectGroup: Base UI Select.Group props plus its label.

```typescript
type SelectGroupProps = {
  /** The group label, in `type-label` caps and --role-muted. */
  label: React.ReactNode;
  className?: string;
};
```

### SelectItemProps

Props for SelectItem: Base UI Select.Item props plus an optional swatch.

```typescript
type SelectItemProps = {
  /** Extra class names, added after the module's own. */
  className?: string;
  /** The product's color (content) as a ringed disc before the label. */
  swatch?: string;
};
```

### SelectOption

One option: its value, its label (the words the trigger shows) and an optional swatch.

```typescript
type SelectOption<Value = string> = {
  value: Value;
  /** The option's words; with a swatch they name the color. */
  label: string;
  /**
   * The product's color, supplied as data like a photograph (a CSS color
   * string); drawn as a ringed disc before the label. Always name the color
   * in `label`.
   */
  swatch?: string;
  disabled?: boolean;
};
```

### SelectOptionGroup

A titled group of options; groups are divided by the popup's separator.

```typescript
type SelectOptionGroup<Value = string> = {
  /** The group label, set in `type-label` caps. */
  label: string;
  items: SelectOption<Value>[];
};
```

### SelectProps

Props for Select: Base UI Select.Root props plus the options, variant and color axes.

```typescript
type SelectProps<Value = string> = (
  { variant?: 'outline'; plate?: boolean } | { variant: 'underline'; plate?: false }
) & {
  /**
   * The options (or titled groups). Rendered as items unless `children`
   * are given; always used to show the chosen option's label.
   */
  items?: SelectItems<Value>;
  /** Custom list content (`SelectItem`, `SelectGroup`, `SelectSeparator`) instead of `items`. */
  children?: React.ReactNode;
  /** Shown in --role-muted while nothing is chosen; end it with "…". */
  placeholder?: string;
  /** Class names for the trigger (the root part), added after the module's own. */
  className?: string;
  /** Names the trigger when no visible label exists. Prefer a `FieldLabel`. */
  'aria-label'?: string;
  /**
   * Primary Radix scale: trigger edge, value, chevron and focus ring. Never
   * defaulted; omitted, it inherits the scope [D133].
   */
  primary?:
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'ruby'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | null;
  /**
   * Secondary Radix scale: the underline variant's underline; the danger
   * scale while invalid.
   */
  secondary?:
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'ruby'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | 'amber'
    | 'blue'
    | 'cyan'
    | 'grass'
    | 'green'
    | 'jade'
    | 'lime'
    | 'mint'
    | 'orange'
    | 'sky'
    | 'teal'
    | 'yellow'
    | null;
};
```

### SelectSeparatorProps

Props for SelectSeparator: Base UI Select.Separator props.

```typescript
type SelectSeparatorProps = { className?: string };
```

## Export Groups

- `Select`: `Select`, `SelectItem`, `SelectGroup`, `SelectSeparator`, `select`, `SelectOption`, `SelectOptionGroup`, `SelectProps`, `SelectItemProps`, `SelectGroupProps`, `SelectSeparatorProps`
- `SelectItem`: `Select`, `SelectItem`, `SelectGroup`, `SelectSeparator`, `select`, `SelectOption`, `SelectOptionGroup`, `SelectProps`, `SelectItemProps`, `SelectGroupProps`, `SelectSeparatorProps`
- `SelectGroup`: `Select`, `SelectItem`, `SelectGroup`, `SelectSeparator`, `select`, `SelectOption`, `SelectOptionGroup`, `SelectProps`, `SelectItemProps`, `SelectGroupProps`, `SelectSeparatorProps`
- `SelectSeparator`: `Select`, `SelectItem`, `SelectGroup`, `SelectSeparator`, `select`, `SelectOption`, `SelectOptionGroup`, `SelectProps`, `SelectItemProps`, `SelectGroupProps`, `SelectSeparatorProps`
