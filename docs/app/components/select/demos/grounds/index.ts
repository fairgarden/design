import { createDemo } from '@/functions/createDemo';
import { SelectGrounds } from './SelectGrounds';

export const DemoSelectGrounds = createDemo(import.meta.url, SelectGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
