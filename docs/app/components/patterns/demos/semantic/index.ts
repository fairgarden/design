import { createDemo } from '@/functions/createDemo';
import { PatternSemantic } from './PatternSemantic';

export const DemoPatternSemantic = createDemo(import.meta.url, PatternSemantic, {
  name: 'Hatch and dot screen',
  slug: 'semantic',
});
