import { createDemo } from '@/functions/createDemo';
import { SectionHeaderKinds } from './SectionHeaderKinds';

export const DemoSectionHeaderKinds = createDemo(import.meta.url, SectionHeaderKinds, {
  name: 'Kinds',
  slug: 'kinds',
});
