import { createDemo } from '@/functions/createDemo';
import { SectionHeaderGrounds } from './SectionHeaderGrounds';

export const DemoSectionHeaderGrounds = createDemo(import.meta.url, SectionHeaderGrounds, {
  name: 'On grounds',
  slug: 'grounds',
});
