import { createDemo } from '@/functions/createDemo';
import { TooltipColor } from './TooltipColor';

export const DemoTooltipColor = createDemo(import.meta.url, TooltipColor, {
  name: 'Primary scale',
  slug: 'color',
});
