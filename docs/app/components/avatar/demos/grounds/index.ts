import { createDemo } from '@/functions/createDemo';
import { AvatarGrounds } from './AvatarGrounds';

export const DemoAvatarGrounds = createDemo(import.meta.url, AvatarGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
