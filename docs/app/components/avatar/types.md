# Avatar

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate components/avatar'

## API Reference

### Avatar

The circular frame and its ring. Compose `AvatarImage` (a photo or a
seal), `AvatarFallback` (1–2 initials, shown until the image loads or if
it fails) and an optional `AvatarStatus`. Give the image alt text with the
name, or empty alt when the name is adjacent. As a link or menu trigger,
wrap it (or pass `render`): on hover a ring up to 40 px steps to
`--primary12` at the same weight.

**Avatar Props:**

| Prop      | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                                                                                                                   |
| :-------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| primary   | `'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'ruby' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale: the ring. Never defaulted; omitted, it inherits&#xA;the scope \[D133].                                                                                                                   |
| secondary | `'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'ruby' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale, accepted for the shared contract; no part uses it.                                                                                                                                     |
| size      | `'xs' \| 'sm' \| 'md' \| 'lg' \| 'xl' \| null`                                                                                                                                                                                                                                                                                                     | -       | `xs` 24, `sm` 32, `md` 40 (default), `lg` 64, `xl` 128 px. Up to 40 px&#xA;the ring is `--border-size-1` `--role-rule`; from 64 px it is&#xA;`--border-size-2` `--primary12`. Sizes are fixed at every width. |

### AvatarFallback

The initials face: a nested white face (`--primary1`) with 1–2 caps
initials in the UI sans at weight 700 and `--primary12`, the same on every
ground. Never color-code initials backgrounds.

### AvatarGroup

Overlapping avatars (−25%) separated by a `--size-px-1` ring in
`--role-halo`, ending in a "+N" initials avatar: 3 avatars at base, up to
5 from 1024 px.

**AvatarGroup Props:**

| Prop      | Type                                                                                                                                                                                                                                                                                                                                               | Default | Description                                                                                                        |
| :-------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------ | :----------------------------------------------------------------------------------------------------------------- |
| moreLabel | `((count: number) => string)`                                                                                                                                                                                                                                                                                                                      | -       | Accessible name for the "+N" avatar; receives N. Default "N more".                                                 |
| primary   | `'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'ruby' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| null`                                                                                                                               | -       | Primary Radix scale for the group's avatars. Never defaulted.                                                      |
| secondary | `'olive' \| 'sage' \| 'slate' \| 'sand' \| 'gray' \| 'mauve' \| 'brown' \| 'bronze' \| 'gold' \| 'red' \| 'ruby' \| 'crimson' \| 'tomato' \| 'pink' \| 'plum' \| 'indigo' \| 'iris' \| 'violet' \| 'purple' \| 'amber' \| 'blue' \| 'cyan' \| 'grass' \| 'green' \| 'jade' \| 'lime' \| 'mint' \| 'orange' \| 'sky' \| 'teal' \| 'yellow' \| null` | -       | Secondary Radix scale, accepted for the shared contract; no part uses it.                                          |
| size      | `'xs' \| 'sm' \| 'md' \| 'lg' \| 'xl' \| null`                                                                                                                                                                                                                                                                                                     | -       | Size of every avatar in the group. Default `md`.                                                                   |
| total     | `number`                                                                                                                                                                                                                                                                                                                                           | -       | How many people the group stands for, when more exist than the avatars&#xA;passed. Default: the number of avatars. |

### AvatarImage

The photo or seal, cropped to the circle. Prints in grayscale.

### AvatarStatus

The status glyph (§1.5.4, with its inner mark) on its own white disc at
the frame's bottom end, `--size-px-3`, in `--role-status` of its status
scale.

**AvatarStatus Props:**

| Prop   | Type     | Default | Description                                                                                                                                 |
| :----- | :------- | :------ | :------------------------------------------------------------------------------------------------------------------------------------------ |
| label  | `string` | -       | The state in words, e.g. "Away": the glyph's accessible name. Show the&#xA;word beside the avatar too; presence is never a color dot alone. |
| status | `Status` | -       | The presence or state: info, success, warning or danger (§1.5.4).                                                                           |

## Additional Types

### avatar

```typescript
type avatar = avatar;
```

### AvatarFallbackProps

Props for AvatarFallback: Base UI Avatar Fallback props.

```typescript
type AvatarFallbackProps = AvatarFallback.AvatarFallbackProps;
```

### avatarGroup

The group's classes: its size publishes the overlap.

```typescript
type avatarGroup = avatarGroup;
```

### AvatarGroupProps

Props for AvatarGroup: `div` props, `render`, the size, the total and the color axes.

```typescript
type AvatarGroupProps = React.ClassAttributes<HTMLDivElement> &
  React.HTMLAttributes<HTMLDivElement> &
  __type & {
    size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl' | null;
    total?: number;
    primary?:
      | 'olive'
      | 'sage'
      | 'slate'
      | 'sand'
      | 'gray'
      | 'mauve'
      | 'brown'
      | 'bronze'
      | 'gold'
      | 'red'
      | 'ruby'
      | 'crimson'
      | 'tomato'
      | 'pink'
      | 'plum'
      | 'indigo'
      | 'iris'
      | 'violet'
      | 'purple'
      | null;
    secondary?:
      | 'olive'
      | 'sage'
      | 'slate'
      | 'sand'
      | 'gray'
      | 'mauve'
      | 'brown'
      | 'bronze'
      | 'gold'
      | 'red'
      | 'ruby'
      | 'crimson'
      | 'tomato'
      | 'pink'
      | 'plum'
      | 'indigo'
      | 'iris'
      | 'violet'
      | 'purple'
      | 'amber'
      | 'blue'
      | 'cyan'
      | 'grass'
      | 'green'
      | 'jade'
      | 'lime'
      | 'mint'
      | 'orange'
      | 'sky'
      | 'teal'
      | 'yellow'
      | null;
    moreLabel?: (count: number) => string;
  };
```

### AvatarImageProps

Props for AvatarImage: Base UI Avatar Image props.

```typescript
type AvatarImageProps = AvatarImage.AvatarImageProps;
```

### AvatarProps

Props for Avatar: Base UI Avatar Root props plus the size and color axes.

```typescript
type AvatarProps = AvatarRootProps & {
  size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl' | null;
  primary?:
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'ruby'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | null;
  secondary?:
    | 'olive'
    | 'sage'
    | 'slate'
    | 'sand'
    | 'gray'
    | 'mauve'
    | 'brown'
    | 'bronze'
    | 'gold'
    | 'red'
    | 'ruby'
    | 'crimson'
    | 'tomato'
    | 'pink'
    | 'plum'
    | 'indigo'
    | 'iris'
    | 'violet'
    | 'purple'
    | 'amber'
    | 'blue'
    | 'cyan'
    | 'grass'
    | 'green'
    | 'jade'
    | 'lime'
    | 'mint'
    | 'orange'
    | 'sky'
    | 'teal'
    | 'yellow'
    | null;
};
```

### AvatarSize

```typescript
type AvatarSize = NonNullable<'xs' | 'sm' | 'md' | 'lg' | 'xl' | null | undefined>;
```

### avatarStatus

The status disc's class.

```typescript
type avatarStatus = avatarStatus;
```

### AvatarStatusProps

Props for AvatarStatus: `span` props, the status and its word.

```typescript
type AvatarStatusProps = {
  /** The presence or state: info, success, warning or danger (§1.5.4). */
  status: Status;
  /**
   * The state in words, e.g. "Away": the glyph's accessible name. Show the
   * word beside the avatar too; presence is never a color dot alone.
   */
  label?: string;
};
```

## External Types

### NonNullable

```typescript
type NonNullable = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
```

### Status

```typescript
type Status = 'info' | 'success' | 'warning' | 'danger';
```

## Export Groups

- `Avatar`: `Avatar`, `AvatarImage`, `AvatarFallback`, `AvatarStatus`, `AvatarGroup`, `avatar`, `avatarStatus`, `avatarGroup`, `AvatarSize`, `AvatarProps`, `AvatarImageProps`, `AvatarFallbackProps`, `AvatarStatusProps`, `AvatarGroupProps`
- `AvatarImage`: `Avatar`, `AvatarImage`, `AvatarFallback`, `AvatarStatus`, `AvatarGroup`, `avatar`, `avatarStatus`, `avatarGroup`, `AvatarSize`, `AvatarProps`, `AvatarImageProps`, `AvatarFallbackProps`, `AvatarStatusProps`, `AvatarGroupProps`
- `AvatarFallback`: `Avatar`, `AvatarImage`, `AvatarFallback`, `AvatarStatus`, `AvatarGroup`, `avatar`, `avatarStatus`, `avatarGroup`, `AvatarSize`, `AvatarProps`, `AvatarImageProps`, `AvatarFallbackProps`, `AvatarStatusProps`, `AvatarGroupProps`
- `AvatarStatus`: `Avatar`, `AvatarImage`, `AvatarFallback`, `AvatarStatus`, `AvatarGroup`, `avatar`, `avatarStatus`, `avatarGroup`, `AvatarSize`, `AvatarProps`, `AvatarImageProps`, `AvatarFallbackProps`, `AvatarStatusProps`, `AvatarGroupProps`
- `AvatarGroup`: `Avatar`, `AvatarImage`, `AvatarFallback`, `AvatarStatus`, `AvatarGroup`, `avatar`, `avatarStatus`, `avatarGroup`, `AvatarSize`, `AvatarProps`, `AvatarImageProps`, `AvatarFallbackProps`, `AvatarStatusProps`, `AvatarGroupProps`
