import { createDemo } from '@/functions/createDemo';
import { LinkGrounds } from './LinkGrounds';

export const DemoLinkGrounds = createDemo(import.meta.url, LinkGrounds, {
  name: 'On paper, forest and leaf',
  slug: 'grounds',
});
