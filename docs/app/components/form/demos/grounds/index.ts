import { createDemo } from '@/functions/createDemo';
import { FormGrounds } from './FormGrounds';

export const DemoFormGrounds = createDemo(import.meta.url, FormGrounds, {
  name: 'On paper and forest',
  slug: 'grounds',
});
