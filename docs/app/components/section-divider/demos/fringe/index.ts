import { createDemo } from '@/functions/createDemo';
import { SectionDividerFringe } from './SectionDividerFringe';

export const DemoSectionDividerFringe = createDemo(import.meta.url, SectionDividerFringe, {
  name: 'The hatched fringe',
  slug: 'fringe',
});
