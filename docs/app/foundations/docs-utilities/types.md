# Docs Utilities

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate foundations/docs-utilities'

## API Reference

### createDemoFactory

The engine's `createDemoFactory` with the design system's defaults
(`DemoContent: DemoLazy`, `DemoContentLoading: DemoLoading`, `DemoTitle`,
`projectDir` / `projectUrl` from `SOURCE_CODE_ROOT_DIR` /
`SOURCE_CODE_ROOT_URL`, `highlightAfter: 'init'`); `options` override any.

**Parameters:**

| Parameter | Type                              | Default | Description |
| :-------- | :-------------------------------- | :------ | :---------- |
| options?  | `Partial<DemoFactoryOptions<{}>>` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = __type;
```

### createDemoWithVariantsFactory

As `createDemoFactory`, over the engine's `createDemoWithVariantsFactory`,
with `fallbackUsesAllVariants: true`: the loading state gets every
variant, so it draws the variant Select (nothing shifts at the swap) and
every variant's `#slug` targets.

**Parameters:**

| Parameter | Type                              | Default | Description |
| :-------- | :-------------------------------- | :------ | :---------- |
| options?  | `Partial<DemoFactoryOptions<{}>>` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = __type;
```

### createMdxComponents

The MDX component map: `pre` (the Code Block, through CodeHighlighter),
`code` (inline code in the syntax tiers), `h1`–`h4` (h2–h4 link to
themselves), `p`, `a` (the design system's Link), `ul`, `ol`, `table`,
`blockquote`, `hr`, and `img` when an `Image` is given. Use it as the
return of your `useMDXComponents`, and as the types factories'
`components`.

**Parameters:**

| Parameter | Type                         | Default | Description |
| :-------- | :--------------------------- | :------ | :---------- |
| options?  | `CreateMdxComponentsOptions` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = MdxComponents;
```

### createMultipleTypesFactory

As `createTypesFactory`, over the engine's `createMultipleTypesFactory`.

**Parameters:**

| Parameter | Type                           | Default | Description |
| :-------- | :----------------------------- | :------ | :---------- |
| options?  | `Partial<TypesFactoryOptions>` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = __type;
```

### createTypesFactory

The engine's `createTypesFactory` with `TypesTable` and `TypePre`; `options` override either.

**Parameters:**

| Parameter | Type                           | Default | Description |
| :-------- | :----------------------------- | :------ | :---------- |
| options?  | `Partial<TypesFactoryOptions>` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = __type;
```

### DemoTitle

The heading the demo factories render for `<DemoX.Title />`: an H3 in
type-subhead that links to itself, so a demo can be linked by its slug.

**DemoTitle Props:**

| Prop     | Type     | Default | Description |
| :------- | :------- | :------ | :---------- |
| slug     | `string` | -       | -           |
| children | `string` | -       | -           |

### Pre

The MDX `pre` override, as fg-docs' own `Pre`: the docs pipeline
(`transformHtmlCodeBlock`) replaces every fenced code block with
`<pre data-precompute=…>`, which only CodeHighlighter can render. Each
renders as a Code Block, code-split, with its loading state until the
chunk loads. A fence's flags (` ```tsx collapse `) arrive in
`data-content-props`.

**Pre Props:**

| Prop               | Type                                           | Default | Description                                                                 |
| :----------------- | :--------------------------------------------- | :------ | :-------------------------------------------------------------------------- |
| Content            | `React.ComponentType<ContentProps<{}>>`        | -       | CodeHighlighter's `Content`. Default: `CodeBlockLazy`.                      |
| ContentLoading     | `React.ComponentType<ContentLoadingProps<{}>>` | -       | CodeHighlighter's `ContentLoading`. Default: `CodeBlockLoading`.            |
| data-content-props | `string`                                       | -       | -                                                                           |
| data-name          | `string`                                       | -       | -                                                                           |
| data-precompute    | `string`                                       | -       | -                                                                           |
| data-slug          | `string`                                       | -       | -                                                                           |
| highlightAfter     | `'init' \| 'stream' \| 'hydration' \| 'idle'`  | -       | When the code highlights. Default: `'init'`, so the server HTML carries it. |

## Additional Types

### createDemo

Creates a demo: the component rendered above its highlighted source.

```typescript
type createDemo = createDemo;
```

### CreateDemo

What `createDemoFactory` returns: `createDemo(url, component, meta?)`.

```typescript
type CreateDemo = ReturnType;
```

### createDemoWithVariants

As `createDemo`, with switchable variants of the same demo.

```typescript
type createDemoWithVariants = createDemoWithVariants;
```

### CreateDemoWithVariants

What `createDemoWithVariantsFactory` returns: `createDemoWithVariants(url, variants, meta?)`.

```typescript
type CreateDemoWithVariants = ReturnType;
```

### CreateMdxComponentsOptions

Options for createMdxComponents. Every one is optional.

```typescript
type CreateMdxComponentsOptions = {
  /**
   * Renders links to internal paths (`/…`), such as next/link, as the
   * design system's Link's element. Default: a plain `a`.
   */
  Link?: MdxLinkComponent;
  /** Renders `img`, such as next/image. Default: the plain element. */
  Image?: React.ElementType<
    ElementProps<'img'>,
    | 'symbol'
    | 'object'
    | 'a'
    | 'abbr'
    | 'address'
    | 'area'
    | 'article'
    | 'aside'
    | 'audio'
    | 'b'
    | 'base'
    | 'bdi'
    | 'bdo'
    | 'big'
    | 'blockquote'
    | 'body'
    | 'br'
    | 'button'
    | 'canvas'
    | 'caption'
    | 'center'
    | 'cite'
    | 'code'
    | 'col'
    | 'colgroup'
    | 'data'
    | 'datalist'
    | 'dd'
    | 'del'
    | 'details'
    | 'dfn'
    | 'dialog'
    | 'div'
    | 'dl'
    | 'dt'
    | 'em'
    | 'embed'
    | 'fieldset'
    | 'figcaption'
    | 'figure'
    | 'footer'
    | 'form'
    | 'h1'
    | 'h2'
    | 'h3'
    | 'h4'
    | 'h5'
    | 'h6'
    | 'head'
    | 'header'
    | 'hgroup'
    | 'hr'
    | 'html'
    | 'i'
    | 'iframe'
    | 'img'
    | 'input'
    | 'ins'
    | 'kbd'
    | 'keygen'
    | 'label'
    | 'legend'
    | 'li'
    | 'link'
    | 'main'
    | 'map'
    | 'mark'
    | 'menu'
    | 'menuitem'
    | 'meta'
    | 'meter'
    | 'nav'
    | 'noindex'
    | 'noscript'
    | 'ol'
    | 'optgroup'
    | 'option'
    | 'output'
    | 'p'
    | 'param'
    | 'picture'
    | 'pre'
    | 'progress'
    | 'q'
    | 'rp'
    | 'rt'
    | 'ruby'
    | 's'
    | 'samp'
    | 'search'
    | 'slot'
    | 'script'
    | 'section'
    | 'select'
    | 'small'
    | 'source'
    | 'span'
    | 'strong'
    | 'style'
    | 'sub'
    | 'summary'
    | 'sup'
    | 'table'
    | 'template'
    | 'tbody'
    | 'td'
    | 'textarea'
    | 'tfoot'
    | 'th'
    | 'thead'
    | 'time'
    | 'title'
    | 'tr'
    | 'track'
    | 'u'
    | 'ul'
    | 'var'
    | 'video'
    | 'wbr'
    | 'webview'
    | 'svg'
    | 'animate'
    | 'animateMotion'
    | 'animateTransform'
    | 'circle'
    | 'clipPath'
    | 'defs'
    | 'desc'
    | 'ellipse'
    | 'feBlend'
    | 'feColorMatrix'
    | 'feComponentTransfer'
    | 'feComposite'
    | 'feConvolveMatrix'
    | 'feDiffuseLighting'
    | 'feDisplacementMap'
    | 'feDistantLight'
    | 'feDropShadow'
    | 'feFlood'
    | 'feFuncA'
    | 'feFuncB'
    | 'feFuncG'
    | 'feFuncR'
    | 'feGaussianBlur'
    | 'feImage'
    | 'feMerge'
    | 'feMergeNode'
    | 'feMorphology'
    | 'feOffset'
    | 'fePointLight'
    | 'feSpecularLighting'
    | 'feSpotLight'
    | 'feTile'
    | 'feTurbulence'
    | 'filter'
    | 'foreignObject'
    | 'g'
    | 'image'
    | 'line'
    | 'linearGradient'
    | 'marker'
    | 'mask'
    | 'metadata'
    | 'mpath'
    | 'path'
    | 'pattern'
    | 'polygon'
    | 'polyline'
    | 'radialGradient'
    | 'rect'
    | 'set'
    | 'stop'
    | 'switch'
    | 'text'
    | 'textPath'
    | 'tspan'
    | 'use'
    | 'view'
  >;
  /** Fenced blocks' CodeHighlighter `Content`. Default: `CodeBlockLazy`. */
  Content?: React.ComponentType<ContentProps<{}>>;
  /** Fenced blocks' CodeHighlighter `ContentLoading`. Default: `CodeBlockLoading`. */
  ContentLoading?: React.ComponentType<ContentLoadingProps<{}>>;
  /** When fenced blocks highlight. Default: `'init'`, so the server HTML carries it. */
  highlightAfter?: 'init' | 'stream' | 'hydration' | 'idle';
  /** More components, or replacements for any of the map's, merged last. */
  components?: Record<string, React.ElementType>;
};
```

### createMultipleTypes

API tables for several related exports, rendered as `<TypesX.Part />`.

```typescript
type createMultipleTypes = createMultipleTypes;
```

### CreateMultipleTypes

What `createMultipleTypesFactory` returns: `createMultipleTypes(url, components, meta?)`.

```typescript
type CreateMultipleTypes = ReturnType;
```

### createTypes

The API table for one export, extracted from TypeScript at build time.

```typescript
type createTypes = createTypes;
```

### CreateTypes

What `createTypesFactory` returns: `createTypes(url, component, meta?)`.

```typescript
type CreateTypes = ReturnType;
```

### DemoFactoryOptions

The engine's demo factory options (`createDemoFactory` from `@fairgarden/docs/abstractCreateDemo`).

```typescript
type DemoFactoryOptions<T extends {} = CodeBlockOptions> = AbstractCreateDemoOptions<T>;
```

### DemoTitleProps

Props the demo factories hand their `DemoTitle`.

```typescript
type DemoTitleProps = { slug?: string; children?: string };
```

### MdxComponents

The map createMdxComponents returns.

```typescript
type MdxComponents = { [key: string]: React.ElementType };
```

### MdxLinkComponent

A link component for internal hrefs, such as next/link.

```typescript
type MdxLinkComponent = React.ComponentType<{
  href: string;
  children?: React.ReactNode;
  className?: string;
}>;
```

### PreProps

What the docs pipeline puts on a fenced block's `pre`, plus the slots.

```typescript
type PreProps = {
  'data-name'?: string;
  'data-slug'?: string;
  'data-precompute'?: string;
  'data-content-props'?: string;
  /** CodeHighlighter's `Content`. Default: `CodeBlockLazy`. */
  Content?: React.ComponentType<ContentProps<{}>>;
  /** CodeHighlighter's `ContentLoading`. Default: `CodeBlockLoading`. */
  ContentLoading?: React.ComponentType<ContentLoadingProps<{}>>;
  /** When the code highlights. Default: `'init'`, so the server HTML carries it. */
  highlightAfter?: 'init' | 'stream' | 'hydration' | 'idle';
};
```

### TypesFactoryOptions

The engine's types factory options (`@fairgarden/docs/abstractCreateTypes`).

```typescript
type TypesFactoryOptions = AbstractCreateTypesOptions;
```

## Export Groups

- `createDemoFactory`: `createDemoFactory`, `createDemoWithVariantsFactory`, `DemoFactoryOptions`, `CreateDemo`, `CreateDemoWithVariants`, `createDemo`, `createDemoWithVariants`, `createTypesFactory`, `createMultipleTypesFactory`, `TypesFactoryOptions`, `CreateTypes`, `CreateMultipleTypes`, `createTypes`, `createMultipleTypes`, `createMdxComponents`, `MdxLinkComponent`, `CreateMdxComponentsOptions`, `MdxComponents`, `DemoTitle`, `DemoTitleProps`, `Pre`, `PreProps`
- `createDemoWithVariantsFactory`: `createDemoFactory`, `createDemoWithVariantsFactory`, `DemoFactoryOptions`, `CreateDemo`, `CreateDemoWithVariants`, `createDemo`, `createDemoWithVariants`, `createTypesFactory`, `createMultipleTypesFactory`, `TypesFactoryOptions`, `CreateTypes`, `CreateMultipleTypes`, `createTypes`, `createMultipleTypes`, `createMdxComponents`, `MdxLinkComponent`, `CreateMdxComponentsOptions`, `MdxComponents`, `DemoTitle`, `DemoTitleProps`, `Pre`, `PreProps`
- `createTypesFactory`: `createDemoFactory`, `createDemoWithVariantsFactory`, `DemoFactoryOptions`, `CreateDemo`, `CreateDemoWithVariants`, `createDemo`, `createDemoWithVariants`, `createTypesFactory`, `createMultipleTypesFactory`, `TypesFactoryOptions`, `CreateTypes`, `CreateMultipleTypes`, `createTypes`, `createMultipleTypes`, `createMdxComponents`, `MdxLinkComponent`, `CreateMdxComponentsOptions`, `MdxComponents`, `DemoTitle`, `DemoTitleProps`, `Pre`, `PreProps`
- `createMultipleTypesFactory`: `createDemoFactory`, `createDemoWithVariantsFactory`, `DemoFactoryOptions`, `CreateDemo`, `CreateDemoWithVariants`, `createDemo`, `createDemoWithVariants`, `createTypesFactory`, `createMultipleTypesFactory`, `TypesFactoryOptions`, `CreateTypes`, `CreateMultipleTypes`, `createTypes`, `createMultipleTypes`, `createMdxComponents`, `MdxLinkComponent`, `CreateMdxComponentsOptions`, `MdxComponents`, `DemoTitle`, `DemoTitleProps`, `Pre`, `PreProps`
- `createMdxComponents`: `createDemoFactory`, `createDemoWithVariantsFactory`, `DemoFactoryOptions`, `CreateDemo`, `CreateDemoWithVariants`, `createDemo`, `createDemoWithVariants`, `createTypesFactory`, `createMultipleTypesFactory`, `TypesFactoryOptions`, `CreateTypes`, `CreateMultipleTypes`, `createTypes`, `createMultipleTypes`, `createMdxComponents`, `MdxLinkComponent`, `CreateMdxComponentsOptions`, `MdxComponents`, `DemoTitle`, `DemoTitleProps`, `Pre`, `PreProps`
- `DemoTitle`: `createDemoFactory`, `createDemoWithVariantsFactory`, `DemoFactoryOptions`, `CreateDemo`, `CreateDemoWithVariants`, `createDemo`, `createDemoWithVariants`, `createTypesFactory`, `createMultipleTypesFactory`, `TypesFactoryOptions`, `CreateTypes`, `CreateMultipleTypes`, `createTypes`, `createMultipleTypes`, `createMdxComponents`, `MdxLinkComponent`, `CreateMdxComponentsOptions`, `MdxComponents`, `DemoTitle`, `DemoTitleProps`, `Pre`, `PreProps`
- `Pre`: `createDemoFactory`, `createDemoWithVariantsFactory`, `DemoFactoryOptions`, `CreateDemo`, `CreateDemoWithVariants`, `createDemo`, `createDemoWithVariants`, `createTypesFactory`, `createMultipleTypesFactory`, `TypesFactoryOptions`, `CreateTypes`, `CreateMultipleTypes`, `createTypes`, `createMultipleTypes`, `createMdxComponents`, `MdxLinkComponent`, `CreateMdxComponentsOptions`, `MdxComponents`, `DemoTitle`, `DemoTitleProps`, `Pre`, `PreProps`
