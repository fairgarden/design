import { createDemo } from '@/functions/createDemo';
import { IconWeights } from './IconWeights';

export const DemoIconWeights = createDemo(import.meta.url, IconWeights, {
  name: 'Rest and emphasis weights',
  slug: 'weights',
});
