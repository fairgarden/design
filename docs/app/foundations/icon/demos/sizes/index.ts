import { createDemo } from '@/functions/createDemo';
import { IconSizes } from './IconSizes';

export const DemoIconSizes = createDemo(import.meta.url, IconSizes, {
  name: 'Size tiers',
  slug: 'sizes',
});
