import { createDemo } from '@/functions/createDemo';
import { IconHover } from './IconHover';

export const DemoIconHover = createDemo(import.meta.url, IconHover, {
  name: 'Hover and press',
  slug: 'hover',
});
