import { createDemo } from '@/functions/createDemo';
import { StickerGrounds } from './StickerGrounds';

export const DemoStickerGrounds = createDemo(import.meta.url, StickerGrounds, {
  name: 'On paper, rose, forest and leaf',
  slug: 'grounds',
});
