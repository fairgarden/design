import { createDemo } from '@/functions/createDemo';
import { SeparatorGrounds } from './SeparatorGrounds';

export const DemoSeparatorGrounds = createDemo(import.meta.url, SeparatorGrounds, {
  name: 'On paper, forest and leaf',
  slug: 'grounds',
});
