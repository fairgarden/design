import { createDemo } from '@/functions/createDemo';
import { SeparatorVariants } from './SeparatorVariants';

export const DemoSeparatorVariants = createDemo(import.meta.url, SeparatorVariants, {
  name: 'Variants and orientation',
  slug: 'variants',
});
