import { createDemo } from '@/functions/createDemo';
import { ExpandingBoxPairs } from './ExpandingBoxPairs';

export const DemoExpandingBoxPairs = createDemo(import.meta.url, ExpandingBoxPairs, {
  name: 'Two pairs and the primary scale',
  slug: 'pairs',
});
