import { createDemo } from '@/functions/createDemo';
import { ButtonGrounds } from './ButtonGrounds';

export const DemoButtonGrounds = createDemo(import.meta.url, ButtonGrounds, {
  name: 'On paper, forest and leaf',
  slug: 'grounds',
});
