import { createDemo } from '@/functions/createDemo';
import { ButtonVariants } from './ButtonVariants';

export const DemoButtonVariants = createDemo(import.meta.url, ButtonVariants, {
  name: 'Variants and sizes',
  slug: 'variants',
});
