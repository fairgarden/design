import { createDemo } from '@/functions/createDemo';
import { ToggleGroups } from './ToggleGroups';

export const DemoToggleGroups = createDemo(import.meta.url, ToggleGroups, {
  name: 'Segmented control and filter chips',
  slug: 'groups',
});
